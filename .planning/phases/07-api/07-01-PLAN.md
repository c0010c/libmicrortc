---
phase: 07-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/api-v1.1-boundary.md
  - docs/api-v1.1-symbol-map.md
autonomous: true
requirements:
  - API-01
  - API-02
  - API-04
  - STYLE-04
must_haves:
  truths:
    - "开发者可以在文档中看到 public、private、test-only 三类 API 边界。"
    - "开发者可以查到 include/micrortc/*.h 中当前 public symbol 的完整旧名到新名映射。"
    - "开发者可以明确看到旧 mrtc_*、MRTC_*、AWS/KVS public 名称默认删除，且不提供兼容 wrapper 或 alias。"
    - "维护者可以看到残留分类规则，供后续扫描脚本和 Phase 8+ 使用。"
  artifacts:
    - path: "docs/api-v1.1-boundary.md"
      provides: "public/private/test-only API 边界契约"
      min_lines: 60
    - path: "docs/api-v1.1-symbol-map.md"
      provides: "include/micrortc/*.h public symbols 旧名到新名映射"
      min_lines: 90
  key_links:
    - from: "docs/api-v1.1-boundary.md"
      to: "cmake/MicroRtcInstall.cmake"
      via: "installed include/ 规则"
      pattern: "include/micrortc|install\\(.*DIRECTORY include"
    - from: "docs/api-v1.1-symbol-map.md"
      to: "include/micrortc/*.h"
      via: "public symbol source rows"
      pattern: "mrtc_peer_connection_create|MRTC_PEER_CONNECTION_HANDLE|rtc_peer_connection_create|RtcPeerConnection"
---

<objective>
冻结 Phase 7 的开发者可读 API 契约文档。

Purpose: 在任何机械改名前，把 version 1.1 的 public/private/test-only 边界、命名规则、旧名迁移映射和删除策略写成 Phase 8+ 可直接消费的事实来源。
Output: `docs/api-v1.1-boundary.md` 和 `docs/api-v1.1-symbol-map.md`。
</objective>

<execution_context>
@/home/qs/dev/libmicrortc/.codex/get-shit-done/workflows/execute-plan.md
@/home/qs/dev/libmicrortc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-api/07-RESEARCH.md
@.planning/phases/07-api/07-VALIDATION.md
@.planning/phases/07-api/07-PATTERNS.md
@CMakeLists.txt
@cmake/MicroRtcInstall.cmake
@cmake/MicroRtcTests.cmake
@include/micrortc/micrortc.h
@include/micrortc/peer_connection.h
@examples/chrome-e2e/mrtc_chrome_answerer.c
@tests/package-consumer/package_consumer.c

<interfaces>
Public API source is installed `include/micrortc/*.h`; current files are exactly `include/micrortc/micrortc.h` and `include/micrortc/peer_connection.h`.

Installed package boundary: `cmake/MicroRtcInstall.cmake` installs `DIRECTORY include/` and exports namespace `micrortc::`.

Library include boundary: `CMakeLists.txt` exposes `include/` as PUBLIC and `src/` as PRIVATE.

Test-only private include boundary: `cmake/MicroRtcTests.cmake` grants `src/` only when `WITH_PRIVATE_INCLUDES` is set; `mrtc_chrome_answerer` also includes `src` privately for E2E harness behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写 public/private/test-only API 边界文档</name>
  <files>docs/api-v1.1-boundary.md</files>
  <read_first>
    - docs/api-v1.1-boundary.md
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/07-api/07-RESEARCH.md
    - .planning/phases/07-api/07-PATTERNS.md
    - CMakeLists.txt
    - cmake/MicroRtcInstall.cmake
    - cmake/MicroRtcTests.cmake
    - include/micrortc/micrortc.h
    - include/micrortc/peer_connection.h
    - examples/chrome-e2e/mrtc_chrome_answerer.c
  </read_first>
  <acceptance_criteria>
    - `docs/api-v1.1-boundary.md` 存在，正文尽可能中文。
    - 文档包含 `Public API Boundary`、`Private API Boundary`、`Test-only / Demo-only Boundary`、`Deletion Policy`、`Residual Classification Contract` 五个章节标题或等价中文标题。
    - public 边界明确限定为 installed `include/micrortc/*.h` 和 CMake package target `micrortc::micrortc`；不把 `src/**/*.h`、E2E answerer private include 或 white-box tests 提升为 public API。
    - 删除策略明确写出旧 `mrtc_*`、`MRTC_*`、AWS/KVS 风格 public names 默认删除，不提供 compatibility wrapper、macro alias 或旧 typedef alias，覆盖 API-04。
    - 残留分类至少列出 `forbidden_public_residual`、`migration_doc_whitelist`、`source_compliance_record`、`test_harness_non_public_api`、`ignored_generated_artifact`，供 STYLE-04 使用。
  </acceptance_criteria>
  <action>创建 `docs/api-v1.1-boundary.md`。按 Phase 7 研究和模式图写中文契约文档：目标、Canonical Sources、public/private/test-only 边界表、删除策略、残留分类规则、Phase 8+ 使用说明。使用 STATE 中锁定决策：public C 函数前缀是 `rtc_*`，public 类型是 `Rtc*`，宏和枚举常量是 `RTC_*`；旧 public names 是迁移/删除目标。不要实现任何 C header 改名，不要新增 wrapper 或 alias 建议；Doxygen/API snapshot/ABI diff 只可标为 REQUIREMENTS 中已延期项目，不能作为本阶段交付。</action>
  <verify>
    <automated>test -f docs/api-v1.1-boundary.md && rg -n 'Public API Boundary|Private API Boundary|Test-only|Demo-only|Deletion Policy|Residual Classification|include/micrortc|src/|WITH_PRIVATE_INCLUDES|micrortc::micrortc|forbidden_public_residual|migration_doc_whitelist|source_compliance_record|test_harness_non_public_api|ignored_generated_artifact' docs/api-v1.1-boundary.md</automated>
    <automated>rg -n 'compatibility wrapper|macro alias|typedef alias|不提供兼容|默认删除' docs/api-v1.1-boundary.md</automated>
  </verify>
  <done>`docs/api-v1.1-boundary.md` 可以作为 API-01、API-04、STYLE-04 的边界事实来源；后续计划无需重新判断哪些 header 是 public。</done>
</task>

<task type="auto">
  <name>Task 2: 编写完整 public symbol 旧名到新名映射</name>
  <files>docs/api-v1.1-symbol-map.md</files>
  <read_first>
    - docs/api-v1.1-symbol-map.md
    - docs/api-v1.1-boundary.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/07-api/07-RESEARCH.md
    - .planning/phases/07-api/07-PATTERNS.md
    - include/micrortc/micrortc.h
    - include/micrortc/peer_connection.h
    - tests/package-consumer/package_consumer.c
  </read_first>
  <acceptance_criteria>
    - `docs/api-v1.1-symbol-map.md` 存在，正文尽可能中文。
    - 文档定义字段：old public symbol、new v1.1 symbol、category、action、source，并说明映射范围只覆盖 installed `include/micrortc/*.h` public symbols。
    - 映射覆盖 `MRTC_STATUS`、全部 `MRTC_STATUS_*`、`mrtc_version_string`、`mrtc_initialize`、`mrtc_shutdown`。
    - 映射覆盖 `peer_connection.h` 中 3 个 opaque handle typedef、所有 public struct/enum typedef、所有 enum constants、全部 public functions，包括 data channel 和 transceiver API。
    - 每行 action 明确为 rename/delete old 且 no wrapper/no alias；新函数使用 `rtc_*`，新类型/typedef/enum type 使用 `Rtc*`，新宏/enum constants 使用 `RTC_*`。
    - 文档不把 `src/` private helpers 作为 public symbols 映射。
  </acceptance_criteria>
  <action>创建 `docs/api-v1.1-symbol-map.md`。按研究文件的 Public Symbol Mapping To Freeze 表逐项落地，不遗漏 `include/micrortc/micrortc.h` 和 `include/micrortc/peer_connection.h` 中的 public symbols。对 opaque handle 使用 `RtcPeerConnection`、`RtcDataChannel`、`RtcRtpTransceiver`；对 status 使用 `RtcStatus`；对 status/header guard helper 使用 `RTC_STATUS_DEFINED`；对 header guards 使用 `RTC_MICRORTC_H` 和 `RTC_PEER_CONNECTION_H`。把 enum constant pattern 展开为可检查的具体旧名/新名行，避免只写一条笼统模式导致 Phase 8 改名时遗漏。不要加入 compatibility wrapper、旧 typedef alias、旧 macro alias 或 CMake target namespace 改名。</action>
  <verify>
    <automated>test -f docs/api-v1.1-symbol-map.md && rg -n 'MRTC_STATUS|RtcStatus|mrtc_version_string|rtc_version_string|MRTC_PEER_CONNECTION_HANDLE|RtcPeerConnection|mrtc_peer_connection_create|rtc_peer_connection_create|MRTC_DATA_CHANNEL_HANDLE|RtcDataChannel|MRTC_RTP_TRANSCEIVER_HANDLE|RtcRtpTransceiver|MRTC_FRAME|RtcFrame|MRTC_CODEC_OPUS|RTC_CODEC_OPUS|MRTC_FRAME_FLAG_KEY_FRAME|RTC_FRAME_FLAG_KEY_FRAME' docs/api-v1.1-symbol-map.md</automated>
    <automated>rg -n 'no wrapper|no alias|不提供兼容|删除旧名|rename/delete' docs/api-v1.1-symbol-map.md</automated>
  </verify>
  <done>`docs/api-v1.1-symbol-map.md` 覆盖 API-02/API-04，并能被 Wave 2 的 residual scan `--check-symbol-map` 作为校验输入。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> docs | 维护者把本地 source/header 事实转写为开发者文档，错误分类会影响 Phase 8+ 改名行为。 |
| docs -> future scripts | Wave 2 扫描脚本会消费本计划产出的分类和 mapping 语义。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `scripts/scan-api-residuals.sh` | mitigate in Plan 02 | 本计划在文档中固定脚本安全要求：quote variables、reject unknown options、avoid `eval`、normalize repo-relative paths；Plan 02 必须实现。 |
| T-07-02 | Information Disclosure | `mrtc-ice-servers.local.json` and reports | mitigate | 边界文档必须声明本地 TURN config 是 local secret，不读取、不打印、不提交；残留分类只记录 path/line/symbol/category/reason，不输出 credential 字段值。 |
| T-07-03 | Repudiation | residual classification docs | mitigate | 残留分类契约必须要求每条 finding 包含 category、matched path、matched symbol、whitelist/source reason，避免过宽白名单造成虚假合规。 |
</threat_model>

<verification>
运行本计划两个 task 的 automated commands。额外执行：
`rg -n 'mrtc_|MRTC_|AWS|KVS' docs/api-v1.1-boundary.md docs/api-v1.1-symbol-map.md` 必须只出现在迁移映射、删除策略、白名单或来源说明上下文中。
</verification>

<success_criteria>
- API-01：`docs/api-v1.1-boundary.md` 明确 public/private/test-only API 边界。
- API-02：`docs/api-v1.1-symbol-map.md` 覆盖 `include/micrortc/*.h` 全部当前 public symbols 的旧名到新名映射。
- API-04：两个文档都明确旧 public names 默认删除且无兼容 wrapper/alias。
- STYLE-04：文档冻结残留分类枚举和 finding 必填字段，为 Wave 2 脚本提供契约。
</success_criteria>

<source_audit>
GOAL: 开发者和维护者可以在改代码前看到冻结的 version 1.1 API 边界、命名规则、旧名迁移映射和删除策略。COVERED by Task 1 and Task 2.
REQ API-01: COVERED by Task 1.
REQ API-02: COVERED by Task 2.
REQ API-04: COVERED by Task 1 and Task 2.
REQ STYLE-04: COVERED by Task 1 classification contract; executable scan covered by Plan 02.
RESEARCH: docs/api-v1.1-boundary.md, docs/api-v1.1-symbol-map.md, no wrapper policy, public header source-of-truth. COVERED.
CONTEXT/STATE decisions: `rtc_*`/`Rtc*`/`RTC_*`, no old compatibility layer, package namespace unchanged. COVERED.
Deferred exclusions: Doxygen/API snapshot/ABI diff, Safari/Firefox, media adapters, signaling adapter. NOT PLANNED.
</source_audit>

<output>
After completion, create `.planning/phases/07-api/07-01-SUMMARY.md`
</output>
