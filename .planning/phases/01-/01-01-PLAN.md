---
phase: 01-
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/01-/BASELINE.md"
  - ".planning/phases/01-/SOURCE-MANIFEST.md"
  - ".planning/phases/01-/COMPLIANCE.md"
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03, BASE-04]
user_setup: []
must_haves:
  truths:
    - "BASE-01, D-15, D-18: BASELINE.md states that `reflib/kvs-webrtc-sdk` is the only stripping baseline and that GitHub upstream latest is not the default reference."
    - "BASE-02, D-17: BASELINE.md records path `reflib/kvs-webrtc-sdk`, version `v1.18.1`, commit `9eebcc4`, and a clean worktree status check."
    - "D-05, D-06, D-07, D-08, D-09, D-10: BASELINE.md classifies modules as `core`, `excluded`, or `supporting/reference-only`, with `Signaling` explicitly excluded."
    - "BASE-04, D-01, D-02, D-03, D-04: SOURCE-MANIFEST.md defines file-level traceability fields `source_path`, `target_path`, `origin_commit`, `derivation`, and `notes` without prelisting all reflib files."
    - "BASE-03, D-11, D-12, D-13, D-14, D-16: COMPLIANCE.md defines Apache-2.0 LICENSE, NOTICE, source attribution, and third-party dependency declaration strategy in the Phase 1 directory."
  artifacts:
    - path: ".planning/phases/01-/BASELINE.md"
      provides: "本地 KVS WebRTC SDK 基线和模块边界记录"
      min_lines: 60
      contains: "reflib/kvs-webrtc-sdk"
    - path: ".planning/phases/01-/SOURCE-MANIFEST.md"
      provides: "后续派生文件来源追溯 manifest 格式"
      min_lines: 45
      contains: "source_path"
    - path: ".planning/phases/01-/COMPLIANCE.md"
      provides: "Apache-2.0、NOTICE 和第三方依赖合规策略"
      min_lines: 50
      contains: "Apache-2.0"
  key_links:
    - from: ".planning/phases/01-/BASELINE.md"
      to: "reflib/kvs-webrtc-sdk"
      via: "local baseline facts"
      pattern: "reflib/kvs-webrtc-sdk"
    - from: ".planning/phases/01-/SOURCE-MANIFEST.md"
      to: "reflib/kvs-webrtc-sdk/src/source"
      via: "source_path field and module classification"
      pattern: "source_path"
    - from: ".planning/phases/01-/COMPLIANCE.md"
      to: "reflib/kvs-webrtc-sdk/CMake/Dependencies"
      via: "third-party dependency table"
      pattern: "OpenSSL|libsrtp|usrsctp"
---

<objective>
建立 Phase 1 的基线、来源追溯和合规边界文档。

Purpose: 后续 Phase 2+ 搬迁、裁剪或重写 AWS KVS WebRTC C SDK 文件时，必须能追溯到本地 `reflib/kvs-webrtc-sdk`，并且不能把 AWS/KVS signaling 或 sample 层误带入核心库。

Output: `.planning/phases/01-/BASELINE.md`、`.planning/phases/01-/SOURCE-MANIFEST.md`、`.planning/phases/01-/COMPLIANCE.md`。
</objective>

<execution_context>
@/home/qs/dev/libmicrortc/.codex/get-shit-done/workflows/execute-plan.md
@/home/qs/dev/libmicrortc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-/01-CONTEXT.md
@.planning/phases/01-/01-RESEARCH.md
@.planning/phases/01-/01-VALIDATION.md
@.planning/phases/01-/01-PATTERNS.md
@reflib/kvs-webrtc-sdk/CMakeLists.txt
@reflib/kvs-webrtc-sdk/src/CMakeLists.txt
@reflib/kvs-webrtc-sdk/LICENSE
@reflib/kvs-webrtc-sdk/NOTICE
@reflib/kvs-webrtc-sdk/CMake/Dependencies
</context>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Baseline facts | mitigate | Executor must verify `git -C reflib/kvs-webrtc-sdk rev-parse --short HEAD` is `9eebcc4` and `git -C reflib/kvs-webrtc-sdk status --short` is empty before documenting the baseline. |
| T-01-02 | Information integrity | Module boundary docs | mitigate | `Signaling` must be documented as `excluded`; `kvsCommonLws` / PIC support must be documented as `supporting/reference-only`, not `core`. |
| T-01-03 | Compliance | LICENSE/NOTICE strategy | mitigate | COMPLIANCE.md must cite `reflib/kvs-webrtc-sdk/LICENSE`, `reflib/kvs-webrtc-sdk/NOTICE`, and third-party dependency declarations from `CMake/Dependencies`. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Reconfirm local baseline facts</name>
  <files>.planning/phases/01-/BASELINE.md</files>
  <read_first>.planning/phases/01-/BASELINE.md, .planning/phases/01-/01-CONTEXT.md, .planning/phases/01-/01-RESEARCH.md, reflib/kvs-webrtc-sdk/CMakeLists.txt, reflib/kvs-webrtc-sdk/src/CMakeLists.txt</read_first>
  <action>Run and record the facts needed by BASELINE.md: `git -C reflib/kvs-webrtc-sdk status --short`, `git -C reflib/kvs-webrtc-sdk rev-parse --short HEAD`, `git -C reflib/kvs-webrtc-sdk describe --tags --always --dirty`, `find reflib/kvs-webrtc-sdk/src/source -maxdepth 1 -type d | sort`, and `rg -n "option\\(|project\\(|add_library\\(|target_link_libraries" reflib/kvs-webrtc-sdk/CMakeLists.txt reflib/kvs-webrtc-sdk/src/CMakeLists.txt`. The target state is path `reflib/kvs-webrtc-sdk`, tag/version `v1.18.1`, commit `9eebcc4`, and clean status with no output from `status --short`.</action>
  <verify>git -C reflib/kvs-webrtc-sdk status --short && git -C reflib/kvs-webrtc-sdk rev-parse --short HEAD && git -C reflib/kvs-webrtc-sdk describe --tags --always --dirty</verify>
  <acceptance_criteria>
    - `git -C reflib/kvs-webrtc-sdk rev-parse --short HEAD` prints `9eebcc4`.
    - `git -C reflib/kvs-webrtc-sdk describe --tags --always --dirty` prints `v1.18.1`.
    - `git -C reflib/kvs-webrtc-sdk status --short` prints no tracked or untracked changes.
  </acceptance_criteria>
  <done>Local baseline facts are rechecked immediately before authoring docs.</done>
</task>

<task type="auto">
  <name>Task 2: Create BASELINE.md</name>
  <files>.planning/phases/01-/BASELINE.md</files>
  <read_first>.planning/phases/01-/BASELINE.md, .planning/phases/01-/01-CONTEXT.md, .planning/phases/01-/01-RESEARCH.md, reflib/kvs-webrtc-sdk/CMakeLists.txt, reflib/kvs-webrtc-sdk/src/CMakeLists.txt</read_first>
  <action>Create `.planning/phases/01-/BASELINE.md` in Chinese. Include sections for baseline summary, reproduction commands, key directory structure, public headers, core/excluded/supporting module classification, key CMake options, source target/link observations, and downstream rules. The module table must classify `Ice`, `Stun`, `Crypto`, `Srtp`, `Sctp`, `Sdp`, `Rtp`, `Rtcp`, and `PeerConnection` as `core`; `Signaling` as `excluded`; `Threadpool`, `Metrics`, `samples/common/StaticMedia.c`, sample H264/Opus frames, `tst/`, and `kvsCommonLws` / PIC capabilities as `supporting/reference-only`. State explicitly that GitHub upstream latest is not the default baseline.</action>
  <verify>test -f .planning/phases/01-/BASELINE.md && rg -n "reflib/kvs-webrtc-sdk|v1.18.1|9eebcc4|Signaling|excluded|supporting/reference-only|GitHub" .planning/phases/01-/BASELINE.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-/BASELINE.md` exists.
    - `BASELINE.md` contains `reflib/kvs-webrtc-sdk`, `v1.18.1`, and `9eebcc4`.
    - `BASELINE.md` contains `Signaling` and `excluded` on module-boundary content.
    - `BASELINE.md` contains `supporting/reference-only`.
    - `BASELINE.md` states that GitHub upstream latest is not the default baseline.
  </acceptance_criteria>
  <done>BASELINE.md captures BASE-01 and BASE-02 with concrete local-source facts.</done>
</task>

<task type="auto">
  <name>Task 3: Create SOURCE-MANIFEST.md</name>
  <files>.planning/phases/01-/SOURCE-MANIFEST.md</files>
  <read_first>.planning/phases/01-/SOURCE-MANIFEST.md, .planning/phases/01-/01-CONTEXT.md, .planning/phases/01-/01-RESEARCH.md, reflib/kvs-webrtc-sdk/src/source</read_first>
  <action>Create `.planning/phases/01-/SOURCE-MANIFEST.md` in Chinese. Define the manifest purpose, update rules, allowed `derivation` values (`copied`, `trimmed`, `renamed`, `rewritten-derived`, `reference-only`, `excluded`), required Markdown table columns `source_path`, `target_path`, `origin_commit`, `derivation`, and `notes`, plus optional columns `module_class`, `requirement`, `phase_added`, and `review_status`. Include an initial module-boundary table, not a full reflib file list. Include a template row showing `reflib/kvs-webrtc-sdk/src/source/Ice/...` to future target `src/...` with origin commit `9eebcc4`, and a row or rule marking `src/source/Signaling` as `excluded`.</action>
  <verify>test -f .planning/phases/01-/SOURCE-MANIFEST.md && rg -n "source_path|target_path|origin_commit|derivation|notes|copied|trimmed|renamed|rewritten-derived|reference-only|excluded|9eebcc4|Signaling" .planning/phases/01-/SOURCE-MANIFEST.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-/SOURCE-MANIFEST.md` exists.
    - `SOURCE-MANIFEST.md` contains all required columns: `source_path`, `target_path`, `origin_commit`, `derivation`, `notes`.
    - `SOURCE-MANIFEST.md` contains derivation values `copied`, `trimmed`, `renamed`, `rewritten-derived`, `reference-only`, and `excluded`.
    - `SOURCE-MANIFEST.md` contains `9eebcc4`.
    - `SOURCE-MANIFEST.md` marks `Signaling` as `excluded`.
  </acceptance_criteria>
  <done>SOURCE-MANIFEST.md establishes BASE-04 file-level traceability rules without prelisting all source files.</done>
</task>

<task type="auto">
  <name>Task 4: Create COMPLIANCE.md</name>
  <files>.planning/phases/01-/COMPLIANCE.md</files>
  <read_first>.planning/phases/01-/COMPLIANCE.md, .planning/phases/01-/01-CONTEXT.md, .planning/phases/01-/01-RESEARCH.md, reflib/kvs-webrtc-sdk/LICENSE, reflib/kvs-webrtc-sdk/NOTICE, reflib/kvs-webrtc-sdk/CMake/Dependencies</read_first>
  <action>Create `.planning/phases/01-/COMPLIANCE.md` in Chinese. Include sections for Apache-2.0 license strategy, NOTICE preservation and libmicrortc derivative notice strategy, source attribution through SOURCE-MANIFEST.md, third-party dependency table, excluded AWS/KVS components, and release checklist. The dependency table must include OpenSSL `OpenSSL_1_1_1t`, libsrtp `bd0f27ec0e299ad101a396dde3f7c90d48efc8fc`, usrsctp `1ade45cbadfd19298d2c47dc538962d4425ad2dd`, libwebsockets `v4.3.5`, jsmn `v1.0.0`, kvsCommonLws/PIC `v1.6.1`, mbedTLS `v2.28.8` or `v3.6.3`, AWS SDK C++ `1.11.741`, googletest `release-1.12.1`, benchmark `v1.5.1`, and gperftools `gperftools-2.8.zip`, each with a v1 status.</action>
  <verify>test -f .planning/phases/01-/COMPLIANCE.md && rg -n "Apache-2.0|NOTICE|SOURCE-MANIFEST.md|OpenSSL_1_1_1t|bd0f27ec0e299ad101a396dde3f7c90d48efc8fc|1ade45cbadfd19298d2c47dc538962d4425ad2dd|v4.3.5|v1.0.0|v1.6.1|1.11.741|release-1.12.1|v1.5.1|gperftools-2.8" .planning/phases/01-/COMPLIANCE.md</verify>
  <acceptance_criteria>
    - `.planning/phases/01-/COMPLIANCE.md` exists.
    - `COMPLIANCE.md` contains `Apache-2.0`, `NOTICE`, and `SOURCE-MANIFEST.md`.
    - `COMPLIANCE.md` contains all listed dependency names and versions/commits.
    - `COMPLIANCE.md` marks AWS SDK C++ and AWS/KVS signaling/service paths as `excluded`.
    - `COMPLIANCE.md` includes a release checklist mentioning root or package `LICENSE` and `NOTICE`.
  </acceptance_criteria>
  <done>COMPLIANCE.md covers BASE-03 with concrete license, NOTICE, attribution, and dependency strategy.</done>
</task>

<task type="auto">
  <name>Task 5: Run final document verification</name>
  <files>.planning/phases/01-/BASELINE.md, .planning/phases/01-/SOURCE-MANIFEST.md, .planning/phases/01-/COMPLIANCE.md</files>
  <read_first>.planning/phases/01-/BASELINE.md, .planning/phases/01-/SOURCE-MANIFEST.md, .planning/phases/01-/COMPLIANCE.md, .planning/phases/01-/01-VALIDATION.md</read_first>
  <action>Run the final Phase 1 validation commands from `01-VALIDATION.md`. If any grep assertion fails, update the relevant document with the exact missing string or table entry. Do not modify `reflib/kvs-webrtc-sdk` during validation.</action>
  <verify>test -f .planning/phases/01-/BASELINE.md && test -f .planning/phases/01-/SOURCE-MANIFEST.md && test -f .planning/phases/01-/COMPLIANCE.md && git -C reflib/kvs-webrtc-sdk status --short && git -C reflib/kvs-webrtc-sdk rev-parse --short HEAD && rg -n "reflib/kvs-webrtc-sdk|9eebcc4|v1.18.1|Signaling|excluded|Apache-2.0|NOTICE|source_path|target_path|origin_commit|derivation|notes" .planning/phases/01-/</verify>
  <acceptance_criteria>
    - All three deliverable documents exist.
    - Final verification command exits 0.
    - `reflib/kvs-webrtc-sdk` remains unchanged after documentation work.
  </acceptance_criteria>
  <done>Phase 1 documentation is command-verifiable and ready for `$gsd-execute-phase 1`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f .planning/phases/01-/BASELINE.md`
- [ ] `test -f .planning/phases/01-/SOURCE-MANIFEST.md`
- [ ] `test -f .planning/phases/01-/COMPLIANCE.md`
- [ ] `git -C reflib/kvs-webrtc-sdk status --short` has no output.
- [ ] `git -C reflib/kvs-webrtc-sdk rev-parse --short HEAD` prints `9eebcc4`.
- [ ] `rg -n "reflib/kvs-webrtc-sdk|9eebcc4|v1.18.1|Signaling|excluded|Apache-2.0|NOTICE|source_path|target_path|origin_commit|derivation|notes" .planning/phases/01-/` exits 0.
</verification>

<success_criteria>

- BASE-01, BASE-02, BASE-03, and BASE-04 are all listed in plan frontmatter.
- BASELINE.md records local path, version, commit, dirty status, module boundary, and CMake/dependency observations.
- SOURCE-MANIFEST.md defines file-level traceability with required fields and does not prelist the full reflib source tree.
- COMPLIANCE.md defines LICENSE, NOTICE, attribution, third-party dependency, and excluded-component strategy.
- `Signaling` is explicitly excluded from core library boundaries.
- The local `reflib/kvs-webrtc-sdk` worktree remains clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-/01-01-SUMMARY.md`.
</output>
