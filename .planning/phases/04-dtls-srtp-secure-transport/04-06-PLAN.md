---
phase: 04-dtls-srtp-secure-transport
plan: 06
type: execute
wave: 6
depends_on:
  - 04-05
files_modified:
  - docs/API-执行器与内存契约.md
  - docs/000-设计边界记录.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05
must_haves:
  truths:
    - "文档明确 Phase 4 使用单一 security_backend vtable，默认构建不强制 OpenSSL/libsrtp"
    - "文档明确 DTLS/SRTP 安全边界、fingerprint mismatch、key export label 和 protect/unprotect 失败语义"
    - "需求追踪和项目状态记录第 4 阶段已规划、准备执行，不宣称已验证或 Chrome E2E 完成"
  artifacts:
    - path: "docs/API-执行器与内存契约.md"
      provides: "安全 backend、DTLS/SRTP、datagram 所有权文档"
      contains: "security_backend"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEC-01..SEC-05 第 4 阶段需求追踪"
      contains: "SEC-05 | 第 4 阶段"
    - path: ".planning/STATE.md"
      provides: "Phase 4 已规划且准备执行的项目状态"
      contains: "第 4 阶段已规划，准备执行"
  key_links:
    - from: "docs/API-执行器与内存契约.md"
      to: "src/security/security.c"
      via: "文档描述 event callback、fingerprint verification、key export 和 SRTP wrapper"
      pattern: "EXTRACTOR-dtls_srtp"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "SEC-01..SEC-05 仍映射第 4 阶段"
      pattern: "SEC-05 | 第 4 阶段"
---

<objective>
收口 Phase 4 文档、需求追踪和项目状态。

Purpose: 让后续执行器和第 5 阶段媒体平面清楚安全边界：第 4 阶段交付 backend 契约和内部 SRTP/SRTCP wrapper，不新增公共媒体 API，不要求真实 Chrome DTLS E2E，不强制 OpenSSL/libsrtp。
Output: 中文契约文档、需求追踪和 STATE/PROJECT 状态更新。
</objective>

<execution_context>
@/home/qs/dev/rtc/.codex/get-shit-done/workflows/execute-plan.md
@/home/qs/dev/rtc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dtls-srtp-secure-transport/04-CONTEXT.md
@.planning/phases/04-dtls-srtp-secure-transport/04-RESEARCH.md
@.planning/phases/04-dtls-srtp-secure-transport/04-VALIDATION.md
@.planning/phases/04-dtls-srtp-secure-transport/04-PATTERNS.md
@.planning/phases/04-dtls-srtp-secure-transport/04-05-SUMMARY.md
@docs/000-设计边界记录.md
@docs/API-执行器与内存契约.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 文档和需求追踪收口</name>
  <files>docs/API-执行器与内存契约.md, docs/000-设计边界记录.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    docs/API-执行器与内存契约.md
    docs/000-设计边界记录.md
    .planning/REQUIREMENTS.md
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/phases/04-dtls-srtp-secure-transport/04-CONTEXT.md
    .planning/phases/04-dtls-srtp-secure-transport/04-RESEARCH.md
  </read_first>
  <action>用中文更新契约文档：说明 `security_backend` 是单一 vtable，DTLS 在 ICE connected 后自动启动，早到 DTLS 被拒绝且不缓存，backend outgoing datagram 通过 `RTC_SECURITY_BACKEND_EVENT_OUTGOING_DATAGRAM` 进入核心并转到既有 `observer.on_datagram`，backend handshake complete 通过 `RTC_SECURITY_BACKEND_EVENT_HANDSHAKE_COMPLETE` 触发 fingerprint verification 和 key export。本地 SDP fingerprint 来自 backend `sha-256`，mismatch 是硬失败，key export label 为 `EXTRACTOR-dtls_srtp`，SRTP/SRTCP wrapper 为内部接口，protect 失败不得输出明文、unprotect 失败不得输出未认证数据。明确默认构建不要求 OpenSSL/libsrtp 开发包，真实适配只作为文档/骨架，不作为默认 build gate；若出现必须说明固定 storage 契约且通过可选编译开关。更新 `.planning/REQUIREMENTS.md` 中 SEC-01..SEC-05 为“已规划”或“已完成”只按当前 GSD 约定选择；如果执行阶段尚未验证，使用“已规划”。更新 `.planning/PROJECT.md` 的阶段规划记录，记录第 4 阶段已规划，不宣称 Chrome E2E 已完成。同步更新 `.planning/STATE.md`：当前焦点切到 Phase 4，计划数量为 6，状态写为“第 4 阶段已规划，准备执行”或等价表述，不写“已验证”或“已完成”。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
    <automated>grep -R "EXTRACTOR-dtls_srtp" docs/API-执行器与内存契约.md && grep -R "SEC-05 | 第 4 阶段" .planning/REQUIREMENTS.md && grep -R "第 4 阶段已规划" .planning/PROJECT.md && grep -R "计划数量：** 6\\|计划数量.*6" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    grep -R "security_backend" docs/API-执行器与内存契约.md
    grep -R "ice_not_connected" docs/API-执行器与内存契约.md
    grep -R "fingerprint_mismatch" docs/API-执行器与内存契约.md
    grep -R "EXTRACTOR-dtls_srtp" docs/API-执行器与内存契约.md
    grep -R "OpenSSL/libsrtp" docs/API-执行器与内存契约.md
    grep -R "SEC-01 | 第 4 阶段" .planning/REQUIREMENTS.md
    grep -R "SEC-05 | 第 4 阶段" .planning/REQUIREMENTS.md
    grep -R "第 4 阶段已规划" .planning/PROJECT.md
    grep -R "计划数量：** 6\\|计划数量.*6" .planning/STATE.md
    ! grep -R "第 4 阶段已验证\\|Chrome 真实 DTLS.*已完成\\|Chrome E2E.*已完成" .planning/PROJECT.md .planning/STATE.md docs/API-执行器与内存契约.md
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>文档和需求追踪准确表达 Phase 4 安全边界，不要求真实 Chrome DTLS E2E，不引入默认第三方依赖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> downstream implementers | 文档错误会导致 Phase 5 泄漏明文或依赖真实库 |
| Planning state -> executor/checker | 状态错误会让执行器误判 Phase 4 已验证或遗漏 04-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Information Disclosure | docs / Phase 5 handoff | mitigate | 文档明确 protect 失败不得输出明文，unprotect 失败不得输出未认证媒体 |
| T-04-20 | Denial of Service | optional real backend | mitigate | 默认构建不依赖 OpenSSL/libsrtp，真实适配必须可选并说明固定 storage |
| T-04-21 | Repudiation | planning status | mitigate | STATE/PROJECT 只声明“已规划，准备执行”，不声明已验证或 Chrome E2E 完成 |
</threat_model>

<verification>
执行 `cmake --build build && ctest --test-dir build --output-on-failure`。执行 acceptance grep，确认 SEC-01..SEC-05 在需求追踪中仍指向第 4 阶段，`.planning/STATE.md` 计划数量为 6，且文档没有宣称 Phase 4 已完成真实 Chrome DTLS E2E。
</verification>

<success_criteria>
- 文档明确固定内存、无线程、用户 UDP/socket、默认无 OpenSSL/libsrtp 依赖。
- 文档明确 backend event、fingerprint mismatch、key export label 和 SRTP/SRTCP wrapper 失败语义。
- 需求追踪和项目状态记录第 4 阶段规划边界；`.planning/STATE.md` 标记为已规划/准备执行，不标记为已验证。
</success_criteria>

<output>
完成后创建 `.planning/phases/04-dtls-srtp-secure-transport/04-06-SUMMARY.md`
</output>
