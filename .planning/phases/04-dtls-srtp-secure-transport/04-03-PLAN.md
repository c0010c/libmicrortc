---
phase: 04-dtls-srtp-secure-transport
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/security/security.h
  - src/security/security.c
  - src/srtp/srtp.h
  - src/srtp/srtp.c
  - src/api/peer_connection.h
  - include/rtc/counters.h
  - include/rtc/trace.h
  - CMakeLists.txt
  - tests/test_security.c
autonomous: true
requirements:
  - SEC-03
  - SEC-04
must_haves:
  truths:
    - "DTLS handshake 完成后自动使用 `EXTRACTOR-dtls_srtp` 导出 keying material"
    - "key export 或 SRTP/SRTCP init 失败会进入安全失败，不能进入 `srtp.ready`"
    - "SRTP/SRTCP protect 失败不输出明文，unprotect 失败不向媒体层输出未认证数据"
  artifacts:
    - path: "src/security/security.c"
      provides: "DTLS complete -> key export -> SRTP ready 编排"
      contains: "EXTRACTOR-dtls_srtp"
    - path: "src/srtp/srtp.c"
      provides: "内部 SRTP/SRTCP protect/unprotect wrapper"
      contains: "rtc_srtp_protect_rtp"
    - path: "tests/test_security.c"
      provides: "key export、protect/unprotect 失败矩阵"
      contains: "key_export_failed"
  key_links:
    - from: "src/security/security.c"
      to: "security backend"
      via: "export_keying_material label"
      pattern: "EXTRACTOR-dtls_srtp"
    - from: "src/srtp/srtp.c"
      to: "observer.on_datagram"
      via: "protect 成功后才允许调用发送回调"
      pattern: "srtp_protect_failed"
---

<objective>
完成 DTLS-SRTP key export 与内部 SRTP/SRTCP protect/unprotect 契约。

Purpose: 满足 SEC-03、SEC-04 与 D-15..D-19：握手完成后自动导出 SRTP keying material 并建立内部媒体层可调用的保护接口，同时严禁明文 RTP/RTCP 泄漏和未认证数据输出。
Output: key export 编排、`srtp.ready` 状态、`src/srtp` 内部 wrapper、确定性错误测试。
</objective>

<execution_context>
@/home/qs/dev/rtc/.codex/get-shit-done/workflows/execute-plan.md
@/home/qs/dev/rtc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dtls-srtp-secure-transport/04-CONTEXT.md
@.planning/phases/04-dtls-srtp-secure-transport/04-RESEARCH.md
@.planning/phases/04-dtls-srtp-secure-transport/04-PATTERNS.md
@.planning/phases/04-dtls-srtp-secure-transport/04-02-SUMMARY.md
@docs/API-执行器与内存契约.md

<interfaces>
本计划只创建内部接口，不新增 public protect/unprotect 用户 API：
```c
rtc_status_t rtc_srtp_protect_rtp(rtc_peer_connection_t *pc,
                                  uint8_t *packet,
                                  size_t *inout_len,
                                  size_t capacity);
rtc_status_t rtc_srtp_unprotect_rtp(rtc_peer_connection_t *pc,
                                    uint8_t *packet,
                                    size_t *inout_len);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: handshake complete 后自动 key export 并进入 srtp.ready</name>
  <files>src/security/security.h, src/security/security.c, src/api/peer_connection.h, include/rtc/counters.h, include/rtc/trace.h, tests/test_security.c</files>
  <read_first>
    .planning/phases/04-dtls-srtp-secure-transport/04-CONTEXT.md
    .planning/phases/04-dtls-srtp-secure-transport/04-RESEARCH.md
    src/security/security.h
    src/security/security.c
    src/api/peer_connection.h
    include/rtc/counters.h
    include/rtc/trace.h
    tests/test_security.c
  </read_first>
  <behavior>
    - deterministic backend handshake complete 后调用 `export_keying_material`，label 精确为 `EXTRACTOR-dtls_srtp`。
    - key export 成功后 observer state 包含 `dtls.connected` 和 `srtp.ready`。
    - key export 失败返回 `RTC_STATUS_BACKEND_ERROR`，reason 为 `key_export_failed`，不进入 `srtp.ready`。
  </behavior>
  <action>按 D-15、D-16 实现 DTLS complete 处理：fingerprint 校验成功后调用 backend `export_keying_material(session, "EXTRACTOR-dtls_srtp", 19, key_block, key_block_len)`。首版 key block 固定为内部常量长度，至少在代码中命名 `RTC_DTLS_SRTP_KEY_MATERIAL_BYTES`，并按 role 保存 local/remote 方向标记，供 deterministic backend 验证。成功后设置 `pc->srtp_ready = 1`，输出 observer state `dtls.connected` 与 `srtp.ready`，trace event `RTC_TRACE_SRTP_STATE` reason `key_export_ok`。失败时设置 DTLS failed/SRTP not ready，observer error subsystem `srtp`、operation `key_export`，trace reason `key_export_failed`，counter `key_export_failed++`。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
    <automated>grep -R "EXTRACTOR-dtls_srtp" src/security/security.c && grep -R "srtp.ready" src/security/security.c tests/test_security.c && grep -R "key_export_failed" src/security/security.c tests/test_security.c</automated>
  </verify>
  <acceptance_criteria>
    grep -R "RTC_DTLS_SRTP_KEY_MATERIAL_BYTES" src/security/security.c src/security/security.h
    grep -R "EXTRACTOR-dtls_srtp" src/security/security.c
    grep -R "srtp.ready" src/security/security.c
    grep -R "key_export_failed" src/security/security.c
    grep -R "key_export_failed" tests/test_security.c
    grep -R "export_keying_material_calls" tests/test_security.c
  </acceptance_criteria>
  <done>key export 成功路径进入 `srtp.ready`，失败路径稳定阻断安全状态。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立内部 SRTP/SRTCP protect/unprotect wrapper</name>
  <files>src/srtp/srtp.h, src/srtp/srtp.c, src/security/security.h, src/security/security.c, CMakeLists.txt, tests/test_security.c</files>
  <read_first>
    src/security/security.h
    src/security/security.c
    src/api/peer_connection.h
    CMakeLists.txt
    tests/test_security.c
    docs/API-执行器与内存契约.md
  </read_first>
  <behavior>
    - `rtc_srtp_protect_rtp` / `rtc_srtp_protect_rtcp` 只有在 `srtp_ready` 时调用 backend protect。
    - protect 失败返回 `RTC_STATUS_BACKEND_ERROR`，counter `protect_failed` 增加，且不会调用 `observer.on_datagram`。
    - unprotect 失败返回 `RTC_STATUS_BACKEND_ERROR` 或 `RTC_STATUS_PROTOCOL_ERROR`，counter `unprotect_failed` 增加，且不会调用 `on_media_frame`。
  </behavior>
  <action>按 D-17、D-18、D-19 新增 `src/srtp/srtp.h` 和 `src/srtp/srtp.c`，只暴露内部函数：`rtc_srtp_protect_rtp`、`rtc_srtp_unprotect_rtp`、`rtc_srtp_protect_rtcp`、`rtc_srtp_unprotect_rtcp`。签名必须携带 `size_t capacity` 用于 protect 输出空间检查；如果 `pc->srtp_ready == 0` 返回 `RTC_STATUS_INVALID_STATE`，reason `srtp_not_ready`。protect 成功后才允许由调用方输出 datagram；本计划可以在测试中通过一个内部 helper 证明 protect 失败不会触发 `observer.on_datagram`。unprotect 失败 reason 使用 `srtp_unprotect_failed` 或 `srtcp_unprotect_failed`；如 backend 暴露 replay failure，映射到 reason `srtp_replay_failed`。不要新增 public `protect_rtp` / `unprotect_rtp` API，不接入 Phase 5 RTP/RTCP media API。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
    <automated>grep -R "rtc_srtp_protect_rtp" src/srtp/srtp.h src/srtp/srtp.c && grep -R "srtp_protect_failed" src/srtp/srtp.c tests/test_security.c && ! grep -R "protect_rtp" include/rtc/peer_connection.h</automated>
  </verify>
  <acceptance_criteria>
    grep -R "rtc_srtp_protect_rtp" src/srtp/srtp.h
    grep -R "rtc_srtp_unprotect_rtcp" src/srtp/srtp.h
    grep -R "size_t capacity" src/srtp/srtp.h
    grep -R "srtp_not_ready" src/srtp/srtp.c
    grep -R "srtp_protect_failed" src/srtp/srtp.c
    grep -R "srtp_unprotect_failed" src/srtp/srtp.c
    grep -R "srtcp_unprotect_failed" src/srtp/srtp.c
  </acceptance_criteria>
  <done>内部 SRTP/SRTCP wrapper 可供 Phase 5 媒体层调用，失败路径不会泄漏明文或未认证数据。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DTLS handshake complete -> SRTP key export | 握手状态转换到密钥材料导出 |
| RTP/RTCP plaintext -> SRTP/SRTCP protect wrapper | Phase 5 内部媒体数据进入保护边界 |
| SRTP/SRTCP ciphertext -> unprotect wrapper | 未认证网络数据进入媒体层前的认证边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | key export | mitigate | label 精确使用 `EXTRACTOR-dtls_srtp`，deterministic backend 验证调用参数 |
| T-04-12 | Information Disclosure | SRTP protect failure | mitigate | protect 失败不调用 `observer.on_datagram`，reason `srtp_protect_failed` |
| T-04-13 | Tampering | SRTP/SRTCP unprotect failure | mitigate | unprotect 失败不调用 media observer，不输出未认证数据 |
| T-04-14 | Denial of Service | protect capacity | mitigate | wrapper 接收 `capacity` 并在调用 backend 前检查输出空间 |
| T-04-15 | Spoofing | replay/auth failure | mitigate | backend replay/auth failure 映射为 `srtp_replay_failed` 或 unprotect failed counter/trace |
</threat_model>

<verification>
执行 `cmake --build build && ctest --test-dir build --output-on-failure`。确认 `include/rtc/peer_connection.h` 没有新增 public `protect_rtp` 或 `unprotect_rtp` API。
</verification>

<success_criteria>
- 成功 handshake 会自动 key export 并进入 `srtp.ready`。
- key export 失败进入安全失败，不进入 `srtp.ready`。
- SRTP/SRTCP protect/unprotect wrapper 存在且为内部接口。
- protect 失败不输出明文 datagram，unprotect 失败不输出未认证媒体。
</success_criteria>

<output>
完成后创建 `.planning/phases/04-dtls-srtp-secure-transport/04-03-SUMMARY.md`
</output>
