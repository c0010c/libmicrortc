# 第 1 阶段：核心骨架与边界契约 - 讨论日志

> **仅用于审计。** 不要把本文档作为 planning、research 或 execution 智能体的输入。
> 决策已经记录在 `01-CONTEXT.md`；本文档只保留讨论过的备选项。

**日期：** 2026-05-10T16:13:14+08:00
**阶段：** 1-核心骨架与边界契约
**已讨论领域：** 公共 API 边界、固定内存与 limits 模型、执行器亲和契约、observer / trace / 计数器形态、项目骨架与测试策略

---

## 公共 API 边界

| 问题 | 选项 | 描述 | 已选 |
|----------|--------|-------------|----------|
| `rtc_peer_connection_config_t` 的组织方式 | A | 顶层总配置 + 子结构 | ✓ |
| `rtc_peer_connection_config_t` 的组织方式 | B | 扁平大结构 | |
| `rtc_peer_connection_config_t` 的组织方式 | C | builder/多步骤配置 | |
| 公开 API 命名 | A | 接近浏览器语义，C 风格命名 | ✓ |
| 公开 API 命名 | B | 更底层库风格 | |
| 公开 API 命名 | C | 极简首批 API | |
| 句柄和生命周期 | A | 不透明句柄 + 用户 arena 内分配对象 | ✓ |
| 句柄和生命周期 | B | 用户提供对象 storage | |
| 句柄和生命周期 | C | 两者都支持 | |
| 后续 API 形状 | A | 占位核心生命周期 API 声明 | ✓ |
| 后续 API 形状 | B | 只声明第 1 阶段真实可用 API | |
| 后续 API 形状 | C | 用文档锁定形状，头文件暂不声明 | |

**用户选择：** 1A、2A、3A、4A  
**备注：** 用户要求解释第 3 题后，确认选择不透明句柄 + 用户 arena 内分配对象。

---

## 固定内存与 limits 模型

| 问题 | 选项 | 描述 | 已选 |
|----------|--------|-------------|----------|
| limits 组织方式 | A | 按子系统分组 | ✓ |
| limits 组织方式 | B | 统一扁平结构 | |
| limits 组织方式 | C | 只暴露高级档位 | |
| 容量不足错误粒度 | A | 精确到资源类别 | ✓ |
| 容量不足错误粒度 | B | 统一容量不足 + diagnostic 字段 | |
| 容量不足错误粒度 | C | 统一容量不足即可 | |
| arena 失败诊断 | A | 返回 required/used 诊断结构 | ✓ |
| arena 失败诊断 | B | 只通过 trace/observer 报告 | |
| arena 失败诊断 | C | 文档给公式，不在运行时返回建议 | |
| 动态内存防线 | A | 必须加入测试或钩子 | ✓ |
| 动态内存防线 | B | 只写设计文档约束 | |
| 动态内存防线 | C | 只在 debug build 检查 | |

**用户选择：** 1A、2A、3A、4A  
**备注：** 固定内存边界需要从第 1 阶段变成可测试契约。

---

## 执行器亲和契约

| 问题 | 选项 | 描述 | 已选 |
|----------|--------|-------------|----------|
| 公开 API 默认调用亲和 | A | 大多数公开 API 要求在 signaling executor 调用 | |
| 公开 API 默认调用亲和 | B | 公开 API 可从任意线程/执行器调用，库内部投递 | |
| 公开 API 默认调用亲和 | C | 按 API 分散 | ✓ |
| UDP datagram 输入 API | A | 必须在 network executor 调用 | ✓ |
| UDP datagram 输入 API | B | 可从任意上下文输入，库转投 network | |
| UDP datagram 输入 API | C | 提供两套 API | |
| observer 回调执行器 | A | 按事件归属触发 | ✓ |
| observer 回调执行器 | B | 全部统一回到 signaling | |
| observer 回调执行器 | C | 用户配置 observer executor | |
| 亲和违规处理 | A | debug 断言 + release 返回稳定错误 | ✓ |
| 亲和违规处理 | B | 只在文档声明，不运行时检查 | |
| 亲和违规处理 | C | 始终 hard assert | |

**用户选择：** 1C、2A、3A、4A  
**备注：** 用户选择比默认推荐更强的分域亲和契约：signaling、media、network API 分别归属对应 executor。

---

## observer / trace / 计数器形态

| 问题 | 选项 | 描述 | 已选 |
|----------|--------|-------------|----------|
| observer API 形态 | A | 细粒度 vtable 回调 | |
| observer API 形态 | B | 统一事件回调 + event union | |
| observer API 形态 | C | 核心细粒度 + 可选统一 adapter | ✓ |
| trace hook 稳定性 | A | 从第 1 阶段就定义稳定事件名和字段键 | ✓ |
| trace hook 稳定性 | B | 先内部调试用，不承诺稳定 | |
| trace hook 稳定性 | C | trace hook 只暴露字符串行 | |
| 计数器读取方式 | A | 快照结构 | ✓ |
| 计数器读取方式 | B | 逐项查询 API | |
| 计数器读取方式 | C | 只通过 observer 事件累计 | |
| 错误事件信息 | A | 稳定错误码 + 子系统 + 操作 + 可选 detail code | ✓ |
| 错误事件信息 | B | 错误码 + 文本消息 | |
| 错误事件信息 | C | 只给 `rtc_status_t` | |

**用户选择：** 1C、2A、3A、4A  
**备注：** 用户先询问推荐答案和理由，随后采纳推荐。

---

## 项目骨架与测试策略

| 问题 | 选项 | 描述 | 已选 |
|----------|--------|-------------|----------|
| 首批目录结构 | A | 常规库结构 | |
| 首批目录结构 | B | 按子系统提前分目录 | ✓ |
| 首批目录结构 | C | 先极简 | |
| 第 1 阶段测试框架 | A | 纯 C 自研极小 test runner | ✓ |
| 第 1 阶段测试框架 | B | CTest + 每个测试一个小可执行文件 | |
| 第 1 阶段测试框架 | C | 引入第三方 C 测试库 | |
| malloc/动态内存防线 | A | 核心代码只走项目 allocator 包装，测试替身统计创建后分配次数 | ✓ |
| malloc/动态内存防线 | B | CI 链接时拦截 malloc/free | |
| malloc/动态内存防线 | C | 只做源码扫描和 code review 规则 | |
| 示例和文档范围 | A | 最小 create/destroy 示例 + API 亲和/内存文档 | ✓ |
| 示例和文档范围 | B | 先不做 example，只做测试 | |
| 示例和文档范围 | C | 提前搭 Chrome 页面 skeleton | |

**用户选择：** 1B、2A、3A、4A  
**备注：** 用户先询问推荐答案和理由，随后采纳推荐。

---

## 智能体的自由裁量

- 具体文件名、内部 helper 拆分、测试 runner 宏名称和 CMake target 名称可由 planner 决定。
- 不允许把 SDP、ICE、DTLS-SRTP、RTP/RTCP 或 Chrome 页面提前纳入第 1 阶段实现范围。

## 延后事项

- 无。讨论保持在第 1 阶段范围内。
