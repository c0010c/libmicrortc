---
phase: 05-rtp-rtcp-media-plane
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/rtcp/rtcp.h
  - src/rtcp/rtcp.c
  - src/media/media.h
  - src/media/media.c
  - src/api/peer_connection.c
  - CMakeLists.txt
  - tests/test_rtcp.c
  - tests/test_main.c
autonomous: true
requirements:
  - RTCP-01
  - RTCP-02
must_haves:
  truths:
    - "RTCP datagram 只有 SRTCP unprotect 成功后才会 parse"
    - "库维护 SR/RR 所需的基础 sender/receiver stats"
    - "SR、RR 和 SDES 可生成、解析并更新 counters"
    - "覆盖 05-CONTEXT D-11、D-15、D-16、D-18、D-19、D-20"
  artifacts:
    - path: "src/rtcp/rtcp.c"
      provides: "RTCP SR/RR/SDES parse/write"
      contains: "rtc_rtcp_write_sender_report"
    - path: "tests/test_rtcp.c"
      provides: "SR/RR/SDES 与 SRTCP protect/unprotect 测试"
      contains: "rtc_test_rtcp"
---

<objective>
建立基础 RTCP SR/RR/SDES 维护能力。

Purpose: 满足 RTCP-01 与 RTCP-02：库基于媒体收发统计生成和解析 Sender Report、Receiver Report 和 SDES，并通过 SRTCP wrapper 保护输出。
</objective>

<context>
@.planning/phases/05-rtp-rtcp-media-plane/05-CONTEXT.md
@.planning/phases/05-rtp-rtcp-media-plane/05-RESEARCH.md
@.planning/phases/05-rtp-rtcp-media-plane/05-PATTERNS.md
@src/srtp/srtp.h
@src/api/peer_connection.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 RTCP SR/RR/SDES packet codec 和 stats 更新</name>
  <files>src/rtcp/rtcp.h, src/rtcp/rtcp.c, src/media/media.h, src/media/media.c, CMakeLists.txt, tests/test_rtcp.c, tests/test_main.c</files>
  <read_first>
    include/rtc/counters.h
    include/rtc/limits.h
    src/media/media.c
    tests/test_rtp.c
  </read_first>
  <action>新增 `src/rtcp/rtcp.h` / `src/rtcp/rtcp.c`。实现 RTCP common header parse/write，支持 packet type 200 SR、201 RR、202 SDES。为每个 media kind 维护 stats：SSRC、packets_sent、octets_sent、packets_received、last_sequence、jitter、last_sr_ntp、last_sr_rtp。实现 `rtc_rtcp_write_sender_report`、`rtc_rtcp_write_receiver_report`、`rtc_rtcp_write_sdes`，输出到固定 buffer；SDES 首版只写 CNAME item，CNAME 长度受 `limits.rtcp.max_sdes_cname_bytes` 限制，默认 CNAME 可使用稳定内部字符串 `rtc-cname` 或 create-time 固定字段。实现 parse SR/RR/SDES，更新 counters：`rtcp_sr_sent`、`rtcp_sr_received`、`rtcp_rr_sent`、`rtcp_rr_received`、`rtcp_sdes_sent`、`rtcp_sdes_received`。测试覆盖 SR/RR/SDES round trip、长度越界、unknown packet type 返回 unsupported/protocol error。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    grep -R "rtc_rtcp_write_sender_report" src/rtcp
    grep -R "rtc_rtcp_write_receiver_report" src/rtcp
    grep -R "rtc_rtcp_write_sdes" src/rtcp
    grep -R "rtcp_sr_sent" include/rtc/counters.h src/rtcp src/media tests/test_rtcp.c
    grep -R "rtcp_rr_received" include/rtc/counters.h src/rtcp src/media tests/test_rtcp.c
    grep -R "rtcp_sdes_received" include/rtc/counters.h src/rtcp src/media tests/test_rtcp.c
    grep -R "rtc_test_rtcp" CMakeLists.txt tests/test_main.c
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>RTCP SR/RR/SDES codec 和 stats/counter 基线可独立测试。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接入 RTCP receive/send 与 SRTCP protect/unprotect</name>
  <files>src/api/peer_connection.c, src/media/media.c, src/rtcp/rtcp.c, tests/test_rtcp.c</files>
  <read_first>
    src/api/peer_connection.c
    src/srtp/srtp.h
    src/srtp/srtp.c
    src/media/media.c
  </read_first>
  <action>修改 `rtc_peer_connection_receive_datagram`：`RTC_NET_PROTOCOL_RTCP` 分支调用 `rtc_media_handle_rtcp_datagram(pc, data, data_len)`。该函数在 network executor 复制 datagram，调用 `rtc_srtp_unprotect_rtcp`，成功后 parse RTCP compound packet；失败时不得 parse 或上报 feedback。新增内部 `rtc_media_send_rtcp_reports`，由 network executor 定时器或测试 helper 触发，按当前 stats 写 SR/RR/SDES compound packet，调用 `rtc_srtp_protect_rtcp`，成功后 `observer.on_datagram` 输出。测试覆盖：收到受保护 RR 后 `srtcp_unprotect_calls == 1` 且 counter 更新；生成 SR/SDES 时 `srtcp_protect_calls == 1` 且 datagram 输出；protect/unprotect 失败时不输出或不 parse。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    grep -R "RTC_NET_PROTOCOL_RTCP" src/api/peer_connection.c
    grep -R "rtc_media_handle_rtcp_datagram" src/api src/media
    grep -R "rtc_srtp_unprotect_rtcp" src/media
    grep -R "rtc_srtp_protect_rtcp" src/media
    grep -R "rtc_media_send_rtcp_reports" src/media tests/test_rtcp.c
    grep -R "srtcp_protect_calls" tests/test_rtcp.c
    grep -R "srtcp_unprotect_calls" tests/test_rtcp.c
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>RTCP receive/send 主链路通过 SRTCP wrapper 集成，失败不输出未保护或未认证数据。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Tampering | RTCP receive | mitigate | SRTCP unprotect 成功后才 parse SR/RR/SDES |
| T-05-11 | Information Disclosure | RTCP send | mitigate | SRTCP protect 成功后才输出 RTCP datagram |
| T-05-12 | Denial of Service | SDES CNAME | mitigate | `max_sdes_cname_bytes` 限制并测试越界 |
</threat_model>

<verification>
执行 `cmake --build build && ctest --test-dir build --output-on-failure`。
</verification>

<success_criteria>
- SR/RR/SDES parse/write 有单测。
- RTCP datagram receive 分支接入 SRTCP unprotect。
- SR/SDES 输出通过 SRTCP protect 后走 `observer.on_datagram`。
- protect/unprotect 失败路径不泄漏或解析未认证数据。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-rtp-rtcp-media-plane/05-04-SUMMARY.md`
</output>
