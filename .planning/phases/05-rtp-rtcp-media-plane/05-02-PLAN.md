---
phase: 05-rtp-rtcp-media-plane
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/rtp/rtp.h
  - src/rtp/rtp.c
  - src/media/media.h
  - src/media/media.c
  - src/api/peer_connection.c
  - src/api/peer_connection.h
  - CMakeLists.txt
  - tests/test_rtp.c
  - tests/test_main.c
autonomous: true
requirements:
  - RTP-01
  - RTP-03
must_haves:
  truths:
    - "Opus frame 发送生成一个 RTP packet，并通过 SRTP protect 后输出 datagram"
    - "H264 access unit 发送支持 single NALU 和 FU-A"
    - "SRTP protect 失败时不得调用 observer.on_datagram 输出明文 RTP"
    - "覆盖 05-CONTEXT D-05、D-06、D-07、D-09、D-10、D-16、D-17、D-19"
  artifacts:
    - path: "src/rtp/rtp.c"
      provides: "RTP header、Opus packetize、H264 single/FU-A packetize"
      contains: "rtc_rtp_packetize_h264"
    - path: "src/media/media.c"
      provides: "media executor 发送入口到 network executor protect/output"
      contains: "rtc_media_send_frame"
    - path: "tests/test_rtp.c"
      provides: "Opus/H264 发送与 SRTP protect 集成测试"
      contains: "rtc_test_rtp"
---

<objective>
打通编码帧到受保护 RTP datagram 的发送路径。

Purpose: 满足 RTP-01 与 RTP-03：用户在 media executor 提交 Opus frame 或 H264 access unit，库生成 RTP、更新 sequence/timestamp，network executor 调用 `rtc_srtp_protect_rtp` 并通过 `observer.on_datagram` 输出。
</objective>

<context>
@.planning/phases/05-rtp-rtcp-media-plane/05-CONTEXT.md
@.planning/phases/05-rtp-rtcp-media-plane/05-RESEARCH.md
@.planning/phases/05-rtp-rtcp-media-plane/05-PATTERNS.md
@.planning/phases/04-dtls-srtp-secure-transport/04-04-SUMMARY.md
@include/rtc/media.h
@src/srtp/srtp.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 RTP header、Opus packetize 和发送 protect/output 链路</name>
  <files>src/rtp/rtp.h, src/rtp/rtp.c, src/media/media.h, src/media/media.c, src/api/peer_connection.c, CMakeLists.txt, tests/test_rtp.c, tests/test_main.c</files>
  <read_first>
    include/rtc/media.h
    src/api/peer_connection.c
    src/srtp/srtp.h
    src/srtp/srtp.c
    tests/test_security.c
  </read_first>
  <action>新增 `src/rtp/rtp.h` / `src/rtp/rtp.c`。定义 RTP 固定头 writer/parser helper：版本固定 2，marker、payload type、sequence number、timestamp、ssrc 明确写入网络字节序。为 Opus 实现 `rtc_rtp_packetize_opus`，输入 `rtc_media_frame_t` 后生成一个 RTP packet，payload 为 `frame->data`，payload type 使用现有 Chrome Opus profile 的 PT（如代码已有常量则复用；否则定义内部常量并在文档中说明），sequence 每包递增，timestamp 使用 `frame->timestamp` 非 0 时的值，否则按 48 kHz 音频 clock 递增 960。新增 `src/media/media.c` 的 `rtc_media_send_frame`，由 `rtc_peer_connection_send_media_frame` 调用；它在 media executor 校验 frame 并复制到固定 slot，然后通过 `pc->executors.network.post` 调用 network task。network task 调用 `rtc_srtp_protect_rtp(pc, packet, &len, capacity)`，成功后调用 `observer.on_datagram`，失败时返回/记录错误且不得输出 datagram。测试必须用 deterministic backend 断言 `srtp_protect_rtp_calls == 1`、`datagram_count == 1`，并断言 protect 失败时 `datagram_count == 0`。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    grep -R "rtc_rtp_packetize_opus" src/rtp
    grep -R "rtc_media_send_frame" src/media src/api
    grep -R "rtc_srtp_protect_rtp" src/media
    grep -R "on_datagram" src/media
    grep -R "RTC_MEDIA_KIND_AUDIO_OPUS" tests/test_rtp.c
    grep -R "srtp_protect_rtp_calls" tests/test_rtp.c
    grep -R "datagram_count" tests/test_rtp.c
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>Opus 发送主链路产生受保护 RTP datagram，protect 失败不泄漏明文。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 H264 access unit single NALU 与 FU-A 发送</name>
  <files>src/rtp/rtp.h, src/rtp/rtp.c, src/media/media.c, tests/test_rtp.c</files>
  <read_first>
    .planning/phases/05-rtp-rtcp-media-plane/05-CONTEXT.md
    include/rtc/media.h
    include/rtc/limits.h
    src/rtp/rtp.c
    src/media/media.c
  </read_first>
  <action>在 `src/rtp/rtp.c` 实现 `rtc_rtp_packetize_h264`。首版支持 Annex B start code access unit 输入：识别 `00 00 01` 和 `00 00 00 01` NALU 边界；单个 NALU payload 长度 `<= limits.rtp.max_payload_bytes` 时生成 single NALU RTP packet；超过容量时生成 FU-A：FU indicator 使用 `F|NRI|28`，FU header 设置 Start/End/R/NAL type，分片大小不得超过 `max_payload_bytes - 2`。同一 access unit 的最后一个 RTP packet marker bit 为 1，前序为 0。若 NALU 数量超过 `max_packets_per_frame`、输入不是 Annex B、payload 容量不足或 fixed slot 不够，返回容量或 unsupported/protocol 错误并 trace reason。测试覆盖：一个小 IDR NALU 生成 single NALU；一个大 NALU 生成多个 FU-A；marker bit 只在最后一包；超过 `max_packets_per_frame` 返回容量；protect 失败不输出明文。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    grep -R "rtc_rtp_packetize_h264" src/rtp
    grep -R "FU-A" src/rtp tests/test_rtp.c
    grep -R "max_packets_per_frame" src/rtp src/media tests/test_rtp.c
    grep -R "max_payload_bytes" src/rtp tests/test_rtp.c
    grep -R "RTC_MEDIA_KIND_VIDEO_H264" tests/test_rtp.c
    grep -R "marker" tests/test_rtp.c
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>H264 发送支持 single NALU 和 FU-A，容量和 protect 失败路径可诊断。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | RTP send | mitigate | `rtc_srtp_protect_rtp` 成功前不调用 `observer.on_datagram` |
| T-05-05 | Denial of Service | H264 packetizer | mitigate | `max_payload_bytes`、`max_packets_per_frame` 和 queue slots 硬限制 |
| T-05-06 | Tampering | RTP header | mitigate | 测试锁定 version、payload type、sequence、timestamp、marker |
</threat_model>

<verification>
执行 `cmake --build build && ctest --test-dir build --output-on-failure`。额外 grep：`grep -R "malloc\\|calloc\\|realloc" src/rtp src/media` 应无新增运行期动态分配。
</verification>

<success_criteria>
- Opus frame 发送生成一个 RTP packet 并输出受保护 datagram。
- H264 access unit 发送支持 single NALU 与 FU-A。
- protect 失败时没有明文 datagram 输出。
- sequence、timestamp、marker、counter 和 trace 有测试覆盖。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-rtp-rtcp-media-plane/05-02-SUMMARY.md`
</output>
