---
phase: 05-rtp-rtcp-media-plane
plan: 05
type: execute
wave: 5
depends_on:
  - 05-04
files_modified:
  - include/rtc/media.h
  - src/rtcp/rtcp.h
  - src/rtcp/rtcp.c
  - src/media/media.c
  - src/api/peer_connection.c
  - tests/test_rtcp.c
  - tests/test_media_api.c
autonomous: true
requirements:
  - RTCP-03
  - RTCP-04
  - OBS-04
must_haves:
  truths:
    - "PLI 发送由用户显式 request_keyframe API 触发，库负责生成受保护 RTCP feedback"
    - "收到远端 PLI 只通过 observer feedback 上报，用户负责编码器关键帧"
    - "NACK 只解析并上报，`retransmit_performed` 必须为 0，不引入重传缓存"
    - "覆盖 05-CONTEXT D-12、D-13、D-14、D-15、D-16、D-18、D-19、D-20"
  artifacts:
    - path: "src/rtcp/rtcp.c"
      provides: "PLI/NACK feedback parse/write"
      contains: "rtc_rtcp_parse_nack"
    - path: "tests/test_rtcp.c"
      provides: "PLI/NACK 反馈与 no-retransmit 测试"
      contains: "nack_no_retransmit"
---

<objective>
实现媒体反馈语义：PLI 可显式请求和接收上报，NACK 只解析上报且不重传。

Purpose: 满足 RTCP-03、RTCP-04 和 OBS-04，确保反馈事件对用户可观测，同时不突破 v1 不做重传/拥塞控制的边界。
</objective>

<context>
@.planning/phases/05-rtp-rtcp-media-plane/05-CONTEXT.md
@.planning/phases/05-rtp-rtcp-media-plane/05-RESEARCH.md
@include/rtc/media.h
@src/rtcp/rtcp.c
@src/media/media.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现显式 PLI 请求和远端 PLI 上报</name>
  <files>src/rtcp/rtcp.h, src/rtcp/rtcp.c, src/media/media.c, src/api/peer_connection.c, tests/test_rtcp.c, tests/test_media_api.c</files>
  <read_first>
    include/rtc/media.h
    include/rtc/peer_connection.h
    src/rtcp/rtcp.c
    src/media/media.c
  </read_first>
  <action>实现 `rtc_peer_connection_request_keyframe(pc, kind)`：只允许在 media executor 调用，`kind` 必须为 `RTC_MEDIA_KIND_VIDEO_H264`，Opus 或非法 kind 返回 `RTC_STATUS_INVALID_ARGUMENT` 或 `RTC_STATUS_UNSUPPORTED`。media 层投递 network task，生成 RTCP payload-specific feedback PLI packet，调用 `rtc_srtp_protect_rtcp`，成功后 `observer.on_datagram` 输出，并增加 `pli_sent` counter。接收方向 parse PLI 后构造 `rtc_media_feedback_t { type = RTC_MEDIA_FEEDBACK_PLI, kind = RTC_MEDIA_KIND_VIDEO_H264, retransmit_performed = 0 }`，通过 `observer.on_media_feedback` 上报，并增加 `pli_received` counter；库不直接控制编码器。测试覆盖 request_keyframe 成功输出 SRTCP datagram、Opus kind 被拒绝、收到远端 PLI 触发 feedback callback。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    grep -R "rtc_peer_connection_request_keyframe" src/api src/media include/rtc/peer_connection.h
    grep -R "RTC_MEDIA_FEEDBACK_PLI" src/rtcp src/media tests/test_rtcp.c
    grep -R "pli_sent" include/rtc/counters.h src/media tests/test_rtcp.c
    grep -R "pli_received" include/rtc/counters.h src/media tests/test_rtcp.c
    grep -R "on_media_feedback" src/media tests/test_rtcp.c
    grep -R "RTC_MEDIA_KIND_VIDEO_H264" tests/test_media_api.c tests/test_rtcp.c
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>PLI 的发送和接收上报语义完整，用户仍负责编码器关键帧。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 NACK 解析上报和 no-retransmit 可观测性</name>
  <files>src/rtcp/rtcp.h, src/rtcp/rtcp.c, src/media/media.c, tests/test_rtcp.c</files>
  <read_first>
    .planning/phases/05-rtp-rtcp-media-plane/05-CONTEXT.md
    include/rtc/media.h
    include/rtc/counters.h
    include/rtc/trace.h
  </read_first>
  <action>实现 `rtc_rtcp_parse_nack`：解析 RTCP transport-layer feedback Generic NACK 的 PID/BLP，把 PID 和 BLP 展开为最多 17 个 sequence number，写入 `rtc_media_feedback_t.lost_sequence_numbers` 和 `lost_sequence_number_count`。收到 NACK 后通过 `observer.on_media_feedback` 上报，设置 `type = RTC_MEDIA_FEEDBACK_NACK`、`retransmit_performed = 0`，增加 `nack_received` 和 `nack_no_retransmit` counters，trace event 使用 `RTC_TRACE_MEDIA_FEEDBACK`，reason 必须包含 `nack_no_retransmit`。不得新增 RTP retransmit cache、RTX、重传 timer 或发送节奏逻辑。测试覆盖 PID-only、PID+BLP 多个 lost sequence、`retransmit_performed == 0`、counter 和 trace reason。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
    <automated>! grep -R "retransmit\\|RTX\\|rtx" src include tests | grep -v "retransmit_performed" | grep -v "nack_no_retransmit"</automated>
  </verify>
  <acceptance_criteria>
    grep -R "rtc_rtcp_parse_nack" src/rtcp
    grep -R "RTC_MEDIA_FEEDBACK_NACK" src/rtcp src/media tests/test_rtcp.c
    grep -R "lost_sequence_numbers" include/rtc/media.h src/rtcp tests/test_rtcp.c
    grep -R "retransmit_performed = 0" src/rtcp src/media tests/test_rtcp.c
    grep -R "nack_received" include/rtc/counters.h src/media tests/test_rtcp.c
    grep -R "nack_no_retransmit" include/rtc/counters.h src/media tests/test_rtcp.c
    grep -R "nack_no_retransmit" src/media tests/test_rtcp.c
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>NACK 反馈可观测，且明确不执行重传。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Reliability | NACK | mitigate | `retransmit_performed = 0`，counter/trace reason 锁定 no-retransmit |
| T-05-14 | Denial of Service | NACK BLP expansion | mitigate | lost sequence 固定数组最多 17 个 |
| T-05-15 | Spoofing | PLI/NACK input | mitigate | 只有 SRTCP unprotect 后的 feedback 才会 parse 和上报 |
</threat_model>

<verification>
执行 `cmake --build build && ctest --test-dir build --output-on-failure`。额外确认未新增重传缓存：`! grep -R "rtx\\|retransmit_cache\\|resend" src include tests`。
</verification>

<success_criteria>
- `request_keyframe(video)` 生成受保护 PLI datagram。
- 收到 PLI 上报 typed feedback。
- 收到 NACK 展开 PID/BLP 并上报 typed feedback。
- NACK 不执行重传，counter/trace 明确 `nack_no_retransmit`。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-rtp-rtcp-media-plane/05-05-SUMMARY.md`
</output>
