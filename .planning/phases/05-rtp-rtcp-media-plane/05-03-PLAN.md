---
phase: 05-rtp-rtcp-media-plane
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/rtp/rtp.h
  - src/rtp/rtp.c
  - src/media/media.h
  - src/media/media.c
  - src/api/peer_connection.c
  - tests/test_rtp.c
autonomous: true
requirements:
  - RTP-02
  - RTP-04
must_haves:
  truths:
    - "RTP datagram 只有 SRTP unprotect 成功后才能进入 depacketize/reassembly"
    - "Opus RTP payload 输出 typed Opus frame"
    - "H264 接收支持 single NALU、FU-A 重组和有限 STAP-A"
    - "覆盖 05-CONTEXT D-04、D-05、D-08、D-09、D-10、D-16、D-18、D-19、D-20"
  artifacts:
    - path: "src/rtp/rtp.c"
      provides: "RTP parse、Opus depacketize、H264 reassembly"
      contains: "rtc_rtp_depacketize_h264"
    - path: "tests/test_rtp.c"
      provides: "Opus/H264 接收和 unprotect 失败测试"
      contains: "unprotect"
---

<objective>
打通受保护 RTP datagram 到 typed media frame observer 的接收路径。

Purpose: 满足 RTP-02 与 RTP-04：network executor 上收到 RTP 后完成 SRTP unprotect，media executor 上 depacketize/reassembly 并输出 Opus frame 或 H264 access unit。
</objective>

<context>
@.planning/phases/05-rtp-rtcp-media-plane/05-CONTEXT.md
@.planning/phases/05-rtp-rtcp-media-plane/05-RESEARCH.md
@.planning/phases/05-rtp-rtcp-media-plane/05-PATTERNS.md
@src/net/demux.c
@src/api/peer_connection.c
@src/srtp/srtp.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 接入 RTP datagram unprotect 与 Opus 接收输出</name>
  <files>src/api/peer_connection.c, src/media/media.h, src/media/media.c, src/rtp/rtp.h, src/rtp/rtp.c, tests/test_rtp.c</files>
  <read_first>
    src/api/peer_connection.c
    src/net/demux.c
    src/srtp/srtp.h
    include/rtc/observer.h
    tests/test_security.c
  </read_first>
  <action>修改 `rtc_peer_connection_receive_datagram`：`RTC_NET_PROTOCOL_RTP` 分支调用 `rtc_media_handle_rtp_datagram(pc, data, data_len)`。该函数必须在 network executor 复制 datagram 到固定 scratch/slot，调用 `rtc_srtp_unprotect_rtp`，成功后解析 RTP header 并投递 media executor task；失败时返回第 4 阶段 wrapper 返回的 status，增加 RTP drop counter，不调用任何 media observer。media executor task 根据 payload type/media kind 输出 typed Opus frame：`frame.kind = RTC_MEDIA_KIND_AUDIO_OPUS`、`frame.data` 指向内部临时输出 buffer 且只在 callback 期间有效、`frame.data_len` 为 RTP payload 长度、`frame.timestamp` 来自 RTP header。测试覆盖：受保护 Opus RTP 输入后 `srtp_unprotect_rtp_calls == 1` 且 `on_media_frame_typed` 收到 Opus；unprotect 返回 protocol/backend error 时 frame callback 计数为 0。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    grep -R "RTC_NET_PROTOCOL_RTP" src/api/peer_connection.c
    grep -R "rtc_media_handle_rtp_datagram" src/api src/media
    grep -R "rtc_srtp_unprotect_rtp" src/media
    grep -R "on_media_frame_typed" src/media tests/test_rtp.c
    grep -R "RTC_MEDIA_KIND_AUDIO_OPUS" src/media tests/test_rtp.c
    grep -R "srtp_unprotect_rtp_calls" tests/test_rtp.c
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>RTP 接收主链路可以输出 typed Opus frame，unprotect 失败不输出未认证数据。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 H264 single NALU、FU-A 和 STAP-A 接收重组</name>
  <files>src/rtp/rtp.h, src/rtp/rtp.c, src/media/media.c, tests/test_rtp.c</files>
  <read_first>
    include/rtc/limits.h
    include/rtc/media.h
    src/rtp/rtp.c
    src/media/media.c
  </read_first>
  <action>实现 `rtc_rtp_depacketize_h264` 和 media 层 H264 reassembly state。single NALU 直接附加到当前 access unit；FU-A start 创建新 NALU，middle 追加 payload，end 完成 NALU；marker bit 为 1 时输出完整 `RTC_MEDIA_KIND_VIDEO_H264` access unit。有限 STAP-A 支持：解析 STAP-A payload 中的 16-bit NALU length，逐个拆出 NALU 并纳入 access unit；长度越界返回 protocol error 并增加 `h264_reassembly_drops`。若 FU-A 缺 start、sequence 不连续、reassembly 超过 `max_reassembly_bytes` 或 marker 到来时 AU 不完整，丢弃当前 AU、trace reason 分别使用 `h264_missing_start`、`h264_sequence_gap`、`h264_reassembly_capacity` 或等价字符串。测试覆盖 single NALU、FU-A 多片重组、STAP-A 两个 NALU、丢片不输出 frame、容量不足计数递增。</action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    grep -R "rtc_rtp_depacketize_h264" src/rtp
    grep -R "STAP-A" src/rtp tests/test_rtp.c
    grep -R "h264_reassembly_drops" src/rtp src/media tests/test_rtp.c
    grep -R "h264_sequence_gap" src/rtp src/media tests/test_rtp.c
    grep -R "RTC_MEDIA_KIND_VIDEO_H264" src/media tests/test_rtp.c
    grep -R "max_reassembly_bytes" src/rtp src/media tests/test_rtp.c
    cmake --build build && ctest --test-dir build --output-on-failure
  </acceptance_criteria>
  <done>H264 接收可以输出完整 access unit，损坏或未认证输入不会触发媒体回调。</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | RTP receive | mitigate | unprotect 失败不 depacketize，不调用 media observer |
| T-05-08 | Tampering | H264 reassembly | mitigate | sequence gap、missing start、capacity 超限时丢弃 AU |
| T-05-09 | Denial of Service | STAP-A parser | mitigate | 每个 NALU length 越界立即 protocol error |
</threat_model>

<verification>
执行 `cmake --build build && ctest --test-dir build --output-on-failure`。额外检查 `grep -R "RTC_NET_PROTOCOL_RTCP" src/api/peer_connection.c` 仍未误路由到 RTP handler。
</verification>

<success_criteria>
- RTP receive 分支接入 `rtc_media_handle_rtp_datagram`。
- Opus depacketize 输出 typed frame。
- H264 single NALU、FU-A、STAP-A 接收路径有测试。
- unprotect、丢片、容量不足不会输出媒体帧。
</success_criteria>

<output>
完成后创建 `.planning/phases/05-rtp-rtcp-media-plane/05-03-SUMMARY.md`
</output>
