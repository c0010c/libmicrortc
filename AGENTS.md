# AGENTS.md

## 适用范围
本文件适用于仓库根目录及其所有子目录。若更深层目录存在新的 `AGENTS.md` 或 `AGENTS.override.md`，以离当前工作目录更近的文件为准。

---

## 1. 项目目标与硬约束

本项目是一个**面向嵌入式设备与资源受限系统的轻量级 WebRTC C 库**。设计参考 KVS WebRTC C SDK 的工程组织方式，但**不追求完整复刻桌面端 WebRTC**。

### 硬约束
1. **可用内存小于 3 MiB 时仍可部署**。
2. **跨平台是一等目标**：必须能落在 Linux、RTOS，以及其他资源受限系统；不能只做 Linux-only 实现。
3. **当前阶段不移植信令部分**。
4. **音频、视频、DataChannel 必须支持独立裁剪开关**。
5. 保持**纯 C、静态链接友好、可交叉编译、可裁剪**。
6. **分层必须清晰**，平台相关逻辑不能污染协议核心。

### 当前阶段范围
核心库聚焦以下数据面与协议面能力：
- PeerConnection 以下的会话装配
- ICE
- DTLS
- SRTP
- RTP / RTCP
- 音频收发（若启用）
- 视频收发（若启用）
- SCTP / DataChannel（若启用）

### 当前阶段非目标
以下内容**不要进入核心库**：
- AWS / KVS 专有信令
- WebSocket / HTTP 信令客户端
- 房间 / 频道管理
- 鉴权流程
- 业务控制面状态机
- 为桌面宿主机调试方便而加入的重依赖或常驻后台模块
- 音视频编解码能力（编码器、解码器、转码器、码率控制器）
- 与具体编解码库的绑定适配层

如样例程序需要信令，只能放在 `samples/`、`examples/` 或独立适配层，且默认不作为目标设备最小交付内容。

### 编解码边界（硬约束）
核心库**不处理任何音视频编解码**。本项目中的核心库只负责 WebRTC 传输、会话装配与协议处理，不负责媒体编解码能力本身。

以下内容**不要进入核心库**：
- 视频编码/解码：H.264、H.265、VP8、VP9、AV1、MJPEG 等
- 音频编码/解码：OPUS、AAC、G.711、G.722、PCM 转码等
- 码率控制、转码、重编码
- 编解码器能力探测与动态装载
- 与 FFmpeg、libavcodec、x264、openh264、opus 等具体编解码库的强绑定
- 为编解码器服务的专用线程、缓冲池、平台适配层

核心库允许保留的内容仅限于：
- RTP / RTCP 收发
- SRTP 加解密
- Payload Type 与媒体类型映射
- 时间戳、序列号、重传、NACK、PLI/FIR 等传输相关机制
- 音频轨 / 视频轨的传输封装
- DataChannel 传输
- SDP 中与媒体传输相关的信息处理

原始媒体帧如何编码、解码、转码，必须由**应用层或核心库之外的独立适配层**负责。

### 冲突时的优先级
**稳定性 > 可移植性 > 内存占用 > 可裁剪性 > 分层清晰度 > 代码整洁度 > 功能丰富度 > 开发便利性**

---

## 2. Codex 工作方式

### 2.1 先读再改
开始修改前，优先阅读以下文件或目录（若存在）：
- `README.md`
- `CMakeLists.txt`
- `cmake/`、`toolchains/`、`configs/`
- `include/`
- `src/`
- `platform/`、`ports/`、`osal/`、`pal/`
- `samples/`、`examples/`
- `tests/`、`tst/`
- `scripts/`

### 2.1A 参考实现优先规则

在开始设计和实现前，必须优先参考：

`/home/qshl/dev/rtc/goodlib/amazon-kinesis-video-streams-webrtc-sdk-c`

实现过程中如有不清晰、不确定的地方，必须继续参考上述库，不得跳过。

如与该库存在冲突、差异，或无法保持一致：
1. 先用通俗的语言解释原因；
2. 说明具体冲突或差异；
3. 询问用户意见后，再继续实现。

### 2.2 最小改动原则
除非任务明确要求，否则：
- 不做大规模重构
- 不同时修改多个无关模块
- 不擅自更换依赖
- 不擅自修改公开 API / ABI / 线程模型
- 不为了某个平台的临时通过而破坏通用架构

### 2.3 复用现有模式
优先复用仓库中已有的：
- 命名风格
- 错误码风格
- `init/deinit`、`create/free` 对称关系
- 日志宏、断言宏、特性开关宏
- 平台抽象接口
- CMake 选项命名和组织方式

### 2.4 每次改动后的输出要求
完成任务后，回答中应尽量包含：
- 修改文件
- 使用的编译开关
- RAM / ROM / 线程 / 缓冲区影响
- 对 Linux / RTOS / 其他平台的影响
- 运行过的构建或测试命令
- 仍未验证的点与风险

---

## 3. 架构与分层要求

### 3.1 推荐分层
依赖方向应尽量保持单向：

`公共 API -> 会话装配 -> 协议核心 -> 媒体/数据通道 -> 平台抽象层 -> 平台端口层`

### 3.2 各层职责
- **公共 API 层**：公开头文件、配置结构、生命周期函数。
- **会话装配层**：PeerConnection、能力开关、配置绑定、对象关系管理。
- **协议核心层**：SDP、ICE、DTLS、SRTP、RTP/RTCP、SCTP/DataChannel 协议处理。
- **媒体/数据通道层**：音频轨、视频轨、DataChannel、时间戳与收发路径。
- **平台抽象层**：内存、时间、锁、线程/任务、socket、随机数、字节序、证书/密钥能力。
- **平台端口层**：Linux/POSIX、RTOS、其他系统的具体实现。
- **样例/测试层**：演示与验证，不得反向依赖到核心设计。
- **信令适配层**：当前阶段不纳入核心库。

### 3.2.1 媒体边界补充
“媒体/数据通道层”中的“音频轨、视频轨”仅指**传输通道与封装边界**，不包含编解码器实现。

该层允许负责：
- 编码后音频包 / 视频包的输入输出
- RTP payload 封装与拆包
- 时间戳与序列号处理
- RTCP 反馈联动

该层不得负责：
- 原始 PCM / YUV 的编码
- H.264 / OPUS 等码流生成或解析
- 编解码参数协商实现
- 解码后渲染前处理

### 3.3 明确禁止的依赖方向
禁止出现：
- 协议核心依赖样例层
- 协议核心直接依赖 Linux/POSIX 具体实现
- 公共 API 暴露平台专用类型
- 业务信令对象下沉到核心协议栈

### 3.4 核心层中的平台禁忌
在核心目录中，除非任务明确要求且仓库已有同类设计，否则不要直接引入：
- `pthread_*`
- `semaphore.h`
- `sys/epoll.h`
- `poll.h`
- `sys/eventfd.h`
- `sys/timerfd.h`
- `unistd.h`
- `fcntl.h`
- `FILE*`
- 环境变量读取
- 命令行参数解析
- Linux 特有 socket 细节
- `errno` 直接作为公共错误语义

需要这些能力时，先下沉到平台抽象层，再由端口层实现。

### 3.5 平台抽象层原则
平台抽象接口必须：
- 窄而稳定
- 不泄露平台细节到公共 API
- 支持 Linux / RTOS / 其他系统分别实现
- 支持裁剪
- 支持必要的外部注入

优先抽象以下能力：
- 内存分配 / 缓冲池
- 时间源 / 定时器
- 锁 / 原子 / 线程或任务
- UDP/TCP 或等价网络收发接口
- 随机数
- 证书 / 密钥 / TLS 后端接入点
- 字节序与平台常量

### 3.6 驱动模型
优先支持**外部 pump / poll / event loop 驱动**，不要强制每个连接都创建内部后台线程。设计上应允许：
- 单线程事件驱动
- work queue 驱动
- RTOS 任务驱动
- 上层主循环主动推进

---

## 4. 裁剪与配置要求

### 4.1 必须支持独立裁剪
音频、视频、DataChannel 必须具备**独立编译期开关**。若仓库尚未统一命名，优先使用：
- `WEBRTC_ENABLE_AUDIO`
- `WEBRTC_ENABLE_VIDEO`
- `WEBRTC_ENABLE_DATA_CHANNEL`

可根据需要继续扩展：
- `WEBRTC_ENABLE_TURN`
- `WEBRTC_ENABLE_IPV6`
- `WEBRTC_ENABLE_STATS`
- `WEBRTC_ENABLE_VERBOSE_LOG`
- `WEBRTC_PORT_POSIX`
- `WEBRTC_PORT_RTOS`

如果仓库已有同类选项，优先沿用现有命名，不要重复造轮子。

### 4.2 裁剪必须是真裁剪
关闭任一模块后，必须一并裁掉：
- 相关源码编译单元
- 相关依赖
- 相关缓冲区 / 对象池
- 相关线程 / 任务
- 默认配置与注册逻辑
- 样例中的默认启用路径

不能只隐藏 API，而让依赖、内存占用或后台逻辑仍然存在。

### 4.3 运行时配置不能替代编译期裁剪
凡是对内存、线程、依赖有明显影响的大模块，优先通过**编译期开关**剔除；运行时配置只用于已启用模块的细节调整。

---

## 5. 资源与实时性约束

### 5.1 内存是硬约束
默认把“**可用内存 < 3 MiB**”当作设计边界。所有实现选择都要优先考虑：
- 峰值内存
- 长连接下的稳定内存
- 高频收发时的内存抖动
- 重连或重新协商时的临时内存
- 不同平台上的最坏情况，而不是只看 Linux 宿主机

### 5.2 明确避免的做法
- 每包一次 `malloc/free`
- 无上限队列、缓存、重传窗口、jitter buffer
- 热路径中的大对象拷贝
- 无上界的临时缓冲
- 递归实现
- 把调试、统计、日志塞进热路径
- 把样例逻辑混入核心收发路径

### 5.3 默认阈值
除非任务明确要求，否则不要新增：
- **大于 4 KiB** 的栈上数组
- **大于 32 KiB** 的静态查找表
- **大于 64 KiB** 的一次性临时堆缓冲

如果必须突破，代码和回答里都要说明：
- 原因
- 生命周期
- 是否可裁剪
- 对 RTOS / 小内存系统的替代方案

### 5.4 队列与缓冲
每个队列、缓存、对象池都必须有：
- 明确容量上界
- 丢弃策略或背压策略
- 生命周期说明

优先单拷贝或零拷贝；能复用缓冲区就不要重复申请。

### 5.5 线程策略
线程越少越好。新增线程前先确认能否：
- 复用现有事件循环
- 改为外部驱动
- 改为任务 / work queue
- 改为非阻塞状态机

若必须新增线程，必须说明：
- 线程职责
- 退出路径
- 栈大小预算
- 锁竞争影响
- 在 RTOS 上的替代实现方式

### 5.6 日志策略
默认日志级别应偏向 `WARN` 或 `ERROR`。详细日志、包级日志、文件日志必须可关闭，且不能让日志路径成为媒体热路径瓶颈。核心库不得强依赖文件系统或标准输出。

---

## 6. API 设计要求

### 6.1 所有权必须显式
每个缓冲区、句柄、上下文都要明确“谁申请、谁释放”。优先使用对称命名：
- `*_init / *_deinit`
- `*_create / *_free`
- `*_open / *_close`
- `*_start / *_stop`

### 6.2 配置必须集中且可检查
- 可调参数优先进入配置结构体
- 每个参数要有默认值、边界检查、单位说明
- 不依赖隐藏全局变量控制行为

### 6.3 API 不能假设 POSIX 运行时
公共 API 不应要求调用方提供以下对象，除非仓库已有明确公开约束且当前任务就是修改该接口：
- `pthread_t`
- 文件描述符
- `FILE*`
- `struct sockaddr*`

优先使用库自己的轻量句柄、配置结构和回调表。

### 6.4 信令 API 保持中立
允许提供：
- local / remote SDP 设置接口
- ICE candidate 注入 / 导出接口
- 连接状态回调
- 错误上报接口

不要在公共 API 中引入：
- AWS/KVS 专有消息结构
- WebSocket 连接对象
- HTTP client 句柄
- 云服务鉴权参数

### 6.5 错误处理可预测
- 不直接 `exit()`、`abort()`
- 不无限重试
- 统一返回明确错误码
- 错误路径必须完整释放资源
- 平台错误码要映射到库内语义，不要直接向外泄露 `errno`

### 6.6 避免隐藏全局状态
除非已有架构明确要求，否则不要新增：
- 隐式单例
- 不可重入全局缓存
- 依赖初始化顺序的全局对象
- 未受保护的全局统计

---

## 7. 可移植性要求

### 7.1 不要默认假设以下条件成立
- 一定有完整 POSIX socket
- 一定有文件系统
- 一定有环境变量
- 一定有阻塞线程等待
- 一定有高精度系统时钟
- 一定有系统证书仓库
- 一定有 OpenSSL 风格完整运行时
- 一定有足量堆内存

### 7.2 条件编译要服务于分层
条件编译应用于：
- 端口实现选择
- 平台差异隔离
- 模块裁剪

不要在核心逻辑中散落大量平台 `#ifdef`。如果某个核心文件里的 `#ifdef` 越来越多，应优先拆分端口实现或抽象接口。

### 7.3 宿主机通过不等于移植完成
Linux 上编译通过或样例跑通，只能说明宿主机端口基本可用；不能据此宣称 RTOS 或其他平台已经完成移植。凡是涉及时间、锁、socket、内存分配、线程模型的改动，都要显式说明其跨平台影响。

---

## 8. 目录与实现组织

优先遵循仓库现有布局；如需新增文件，靠近现有模块，不随意新增顶层目录。

若仓库采用类似以下布局，则按职责落位：
- `include/`：公开头文件
- `src/`：核心实现
- `platform/` / `ports/` / `osal/` / `pal/`：平台抽象与端口实现
- `samples/` / `examples/`：样例
- `tests/` / `tst/`：测试
- `scripts/`：构建、检查、辅助脚本
- `toolchains/`：交叉编译工具链文件

不要把以下内容混入核心库目录：
- 样例快捷实现
- 测试夹具
- 板级 hack
- 驱动 glue code
- 私有信令流程

---

## 9. 依赖策略

### 9.1 默认原则
- 尽量复用现有依赖
- 不轻易新增第三方库
- 新依赖必须评估 ROM / RAM / 线程 / 可交叉编译性 / 可裁剪性

### 9.2 能不用重依赖就不用
不要为了小功能引入完整：
- JSON 框架
- 异步 runtime
- 事件循环框架
- 大型日志框架
- 通用容器库

若某依赖仅服务于样例或宿主机测试，应限制在样例/测试目标中，不得成为核心库硬依赖。

### 9.3 crypto / TLS / 证书后端要可替换
如接入 DTLS、SRTP、随机数、证书相关第三方实现，必须尽量通过窄接口封装，为未来更换 TLS/crypto 后端或接入平台安全模块保留空间。

---

## 10. 构建、测试与验证

### 10.1 优先使用仓库现有命令
若仓库已提供标准命令，优先使用现有命令。若无项目特定命令，可用以下最小流程：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j
ctest --test-dir build --output-on-failure
```

### 10.2 默认验证要求
修改完成后，至少做以下之一：
- 宿主机构建通过
- 相关单元测试通过
- 对应样例可编译
- 对应 feature-gate 关闭时仍可编译

### 10.3 feature-gate 相关改动必须额外检查
若修改涉及裁剪开关，应尽量验证以下组合中的相关项：
- 仅音频
- 仅视频
- 仅 DataChannel
- 音频 + 视频
- 音频 + DataChannel
- 视频 + DataChannel
- 全关后核心最小构建（若架构允许）

无法全测时，至少验证本次改动直接影响到的组合，并明确写出未验证项。

### 10.4 平台相关改动必须说明边界
如果当前环境只能验证 Linux/POSIX：
- 明确写出“仅验证宿主机端口”
- 不要默认宣称 RTOS 可用
- 说明 RTOS / 其他系统可能受影响的接口点

---

## 11. 文档与提交前自检

### 11.1 这些变更需要同步更新文档或注释
- 公开 API
- 默认配置
- 编译开关
- 目录结构
- 平台移植要求
- 内存 / 线程 / 缓冲模型
- 端口层接口契约
- 模块裁剪行为

### 11.2 提交前自检清单
- [ ] 只修改与任务直接相关的文件
- [ ] 没有破坏现有命名和资源释放对称性
- [ ] 所有新增缓冲区都有明确上界
- [ ] 没有引入无上限内存增长路径
- [ ] 没有新增不必要的线程
- [ ] 错误路径可以正确释放资源
- [ ] 没有把 Linux/POSIX 专用逻辑直接写进协议核心
- [ ] 平台抽象层与端口层边界仍清晰
- [ ] 音频 / 视频 / DataChannel 关闭后仍可独立构建
- [ ] 没有新增对文件系统、环境变量、POSIX API 的隐式硬依赖
- [ ] 没有把音视频编解码逻辑、编解码依赖或编解码线程引入核心库

---

## 12. 回答模板

完成编码任务后，优先按以下结构汇报：
1. 变更摘要
2. 修改文件
3. 编译开关
4. 资源影响（RAM / ROM / 线程 / 缓冲区）
5. 平台影响（Linux / RTOS / 其他）
6. 验证命令与结果
7. 风险与未验证项

无法验证的内容必须明确写“未验证”，不要默认视为已完成。
