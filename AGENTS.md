# AGENTS.md

## 项目定位
- 本项目是运行在嵌入式设备上的 WebRTC C 语言库。
- 目标平台包括 RTOS、裁剪 Linux 和其他资源受限硬件。
- 可用内存小于 3 MB。
- 第一优先级：正确性、可观测性、资源有界、行为可预测。
- 不要按“桌面/服务器程序”的假设写代码。

## 硬性规则
- 每次实现需求、修复 bug、重构、优化前，必须先参考指定目录中的现有实现，再开始修改。
- 优先复用现有模式、接口风格、状态机、错误处理、日志风格、内存管理方式。
- 没有充分理由，不得引入新的实现套路。
- 永远优先考虑可观测性；关键状态变化、失败路径、资源耗尽、协议异常必须可定位。
- 改动必须尽量小，只解决当前问题，不顺手重构无关代码。

## 实现前强制步骤
开始任何改动前，必须先检查以下目录中的相关实现（参考权重按顺序从大到小）,如果实现相同，则直接实现，否则告诉用户差异，让用户抉择：

- `/home/qshl/dev/rtc/goodlib/libdatachannel`
- `/home/qshl/dev/rtc/goodlib/amazon-kinesis-video-streams-webrtc-sdk-c`
- `/home/qshl/dev/rtc/goodlib/libpeer`
- `/home/qshl/dev/rtc/goodlib/metaRTC`

必须先找相似实现，再编码。优先参考：
- 同类状态机
- 同类收发路径
- 同类缓冲区管理
- 同类超时/重试逻辑
- 同类日志与错误处理

如果参考目录里已有相近实现，必须优先对齐，不能另起一套。

## 模块分层约束
代码设计必须贴近以下分层，避免跨层污染：

### 1. platform
职责：
- OS 抽象
- 时间、锁、线程/任务、socket、随机数、内存、日志输出接口

规则：
- 平台相关代码只放在 platform 层
- 上层不得直接依赖具体 RTOS / Linux 细节
- 不要把平台分支散落到协议层和媒体层

### 2. transport
职责：
- UDP/socket 收发
- 基础收包、发包、地址管理
- 与平台网络接口对接

规则：
- transport 层不处理复杂协议状态机
- 必须明确收发缓冲生命周期
- 尽量减少拷贝

### 3. ice
职责：
- candidate、candidate pair、connectivity check
- ICE 状态推进
- 超时、重试、选路

规则：
- 所有状态切换必须清晰可追踪
- 超时、失败、重试原因必须可观测
- 队列、检查列表、缓存必须有硬上限

### 4. dtls
职责：
- DTLS 握手
- 密钥协商输入输出
- 与 SRTP 建立衔接

规则：
- 握手阶段必须可定位
- 失败原因必须可定位
- 不要把 DTLS 细节泄漏到其他层

### 5. srtp
职责：
- SRTP 上下文建立
- RTP/RTCP 加解密保护

规则：
- key 生命周期必须清晰
- 激活、失败、重建过程必须可观测
- 不得出现不清晰的上下文所有权

### 6. rtp / rtcp
职责：
- RTP/RTCP 包处理
- 序列号、时间戳、统计、反馈

规则：
- 解析失败、乱序、丢包、异常包必须可诊断
- 不要在该层引入平台依赖
- 缓冲与重组必须有上限

### 7. session / peer_connection
职责：
- 会话级状态组织
- 各模块连接关系管理
- 生命周期管理

规则：
- 只做编排，不吞掉底层错误
- 生命周期边界必须清晰
- 销毁路径必须完整可验证

## 内存与性能硬约束
- 默认避免堆分配；除非已有模式明确允许且必要。
- 优先使用固定大小缓冲区、对象池、环形缓冲区、调用方提供内存。
- 禁止无界增长的数据结构。
- 每个队列、缓存、重传列表、等待列表都必须有最大长度。
- 禁止递归。
- 避免大栈对象。
- 禁止隐藏分配。
- 热路径避免不必要拷贝、重复扫描、重格式化、重日志。
- 不要引入后台线程/任务，除非任务明确要求且仓库已有同类实现。

## 可观测性硬约束
以下事件必须具备可定位能力：
- 初始化 / 反初始化
- 会话创建 / 销毁
- ICE 状态变化
- DTLS 握手阶段变化
- SRTP 建立 / 失败
- RTP / RTCP 解析异常
- 丢包、重传、乱序、超时
- 内存池耗尽、分配失败
- 队列满、缓存满、背压触发
- 非法输入、协议违例、异常对端行为

日志要求：
- 必须分级：ERROR / WARN / INFO / DEBUG
- DEBUG 必须可关闭或可裁剪
- 热路径日志必须谨慎，必要时限频
- 日志必须带上下文，至少尽量包含：
    - 模块名
    - 会话/连接标识
    - 状态变化
    - 错误码
    - 关键参数
- 不允许只有“失败了”这类无上下文日志

能低成本加上的观测项，优先保留或补充：
- 错误计数
- 丢包计数
- 重传计数
- 队列深度
- 内存池水位
- 关键状态快照

## 错误处理硬约束
- 所有返回值必须检查。
- 所有错误路径必须显式处理。
- 不允许静默吞错。
- 清理路径必须完整，防止泄漏、重复释放、悬空指针。
- 网络输入、协议输入、平台返回值、外部回调默认都不可信。
- 所有长度、边界、状态一致性都必须检查。

## 禁止事项
- 禁止跳过“先参考指定目录实现”。
- 禁止为了小需求大改架构。
- 禁止引入重量级依赖。
- 禁止引入 Linux-only 假设，除非任务明确限定。
- 禁止引入无界内存使用。
- 禁止破坏现有状态机风格和模块边界。
- 禁止为了表面简洁牺牲可观测性。
- 禁止在热路径加入不可关闭的高频日志。

## 完成任务时必须说明
- 参考了哪些目录、文件、函数或模式
- 改动遵循了哪些现有实现
- 改动影响了哪些模块
- 是否增加了内存开销
- 是否影响实时性
- 增加或保留了哪些日志、计数器、状态观测点

## 决策优先级
当有多种实现方案时，按以下顺序选择：
1. 与参考目录现有实现一致
2. 可观测性更强
3. 内存更小且有界
4. 行为更可预测
5. 改动更小、风险更低
6. 更易移植到 RTOS / 裁剪 Linux / 受限硬件
7. 性能更稳定